# SIFT 1.0 default contract — implementation review

Objective: implement the owner-approved `docs/specs/1.0-compatibility-proposal.md` and deliver a tested draft PR. Version, release, merge, and publication actions remain separate; 1.0 has not shipped.

Decisions implemented: only the Stability/permutation/CatBoost `random_state=None` defaults become 0; public `verbose=True` becomes false; their `n_jobs=-1` defaults become 1; ten selector transformer defaults become `output_order="original"`. Explicit None, explicit legacy order, parallel options, nested learned-prefix order, seed-42 defaults, filter/cache seed semantics, all 66 exports, returns, aliases, sklearn 1.3 floor, and algorithm/count defaults remain. CatBoost dictionaries still win collisions and warn accurately. Experimental and ledger messaging matches retained imports and policy.

State: draft PR #101 is open and unmerged. Its original exact head `a2da7e3` passed all six active CI jobs. Grok's adversarial review found no runtime defect; independent verification found three bounded documentation issues. Commits `0d8b143` and `7ee073d` clarify the CatBoost collision warning, remove stale wrapper `FutureWarning` sections, and correct inherited sampling-preset `verbose` documentation. Runtime evidence was rerun from clean commit `7ee073d`; all 18 data and selection fingerprints are unchanged, only the expected two source hashes changed, and frozen bakeoff evidence remains untouched. Focused review verification: 265 passed, 13 skipped; Ruff, generated API reference, and `git diff --check` pass. The earlier full suite remains 2,786 passed, 41 skipped.

Next: commit and push the review refresh to the draft PR, await all six exact-head CI jobs, repair only a demonstrated failure, then callback to Astra. Do not mark ready, merge, version, or release.
