# SIFT 0.9 worklog

- Objective: Repair the verified 0.9 correctness/contract gaps, obtain the campaign's first CI evidence, and reach an honest no-PyPI 0.9.0 release-ready state.
- Latest steering: Update the consolidated plan and campaign spec for Grok's verified sequencing, compatibility, and target-encoding corrections.
- Constraints: Keep sklearn `>=1.3,<2`, existing unsafe encoders behind `allow_full_data_target_encoding=True`, fixed-k rejection of `groups`/`time`, the 58-name top-level surface, and current default `cat_encoding="none"`. One-hot remains blocked on F3 block-aware selection.
- Decisions: `target_cv` must emit fold-centered effects on training and inference paths so unique levels are neutral rather than fold markers. New target-derived encoding is rejected for knockoffs because it invalidates the Model-X claim. Honest `input_kind="unknown"` and the row-preserving warmup-policy alias remain; large module splits wait until after correctness and CI.
- Completed: Workstream D is committed at `8263e00`. E1 is committed at `63e32c3`. E2 adds weighted, grouped, and temporal `target_cv` encoding across function filters, nested auto-k evaluation, selector wrappers, binary CEFS+, Boruta, and knockoffs, with target-blind inference; its leakage and metadata corrections are now Stage 1 work.
- Current state: E2 is committed, but the unique-ID acceptance guarantee is false: a reproduced eight-seed mRMR fixture selected the ID in 7/8 seeds. The consolidated execution plan is `docs/specs/0.9-next-steps.md`; no 0.9 commit has run on GitHub CI.
- Next action: Implement Stage 1 of the consolidated plan: centered target encoding and metadata, sklearn contracts, and result views; then open the first CI-backed PR. Deprecation-policy cleanup is a Stage 3 release-readiness task.
- Blockers: Do not push/tag/release before Stage 1 passes. Do not create a GitHub Release before the asset-only workflow replaces the still-live remote PyPI workflow.
- Decisive verification: The prior E2 suites remain green, but stronger probes reproduce the fold-prior leak, duck-array sklearn crash in both supported environments, stray target-CV metadata keys, DataFrame `feature_names_in_` type failure, Stability view ordering mismatch, missing Auto-K curves, and missing knockoff raw width. These failures define Stage 1's gate.

## Entry — adversarial review round on PR #72 (`stage1/0.9-corrections`, base `4de1957`)

- Objective: Close the six findings of an independent adversarial review of PR #72, and record Stage 1/Stage 2 completion honestly.
- Latest steering: Fix every finding; do not change selection numerics for finding 2 — fix the over-claim instead.
- Completed: Stages 1 and 2 are now marked complete in `docs/specs/0.9-next-steps.md`, with the PR #72 CI evidence on `4de1957` (test 3.10/3.11/3.12, test-catboost, min-pins, wheel-smoke all green) and the four CI-only fixes that were needed (full-history checkout for the gate-provenance test, the nested auto-k stub keyword, CatBoost seed warnings, platform-stable gate rendering). A review-round table records all six findings and their resolution.
- Decisions: `target_cv_smoothing="auto"` is now valid on the weighted, grouped, and time-aware paths — the empirical-Bayes prior is the unweighted formula with counts replaced by weighted row mass, so it was always well defined there and the gate was simply wrong. No contextual case was found in which `"auto"` is undefined but an explicit float is not; the only genuinely undefined case, a fitting slice with no positive weight mass, still raises for both and is pinned by a test. Near-unique identifiers keep their current numerics: a level appearing twice in training legitimately carries its sibling row's target, so this is documented as the boundary of the centering guarantee (drop ID-like columns, or pass `groups=`) rather than engineered away.
- Current state: Six findings fixed. `TargetCVEncoder.fit()` and `fit_transform` now share `_active_split_count`, so both report the split count over active rows. The stale 3.13 workflow comment is rewritten and the `test-catboost` marker check counts each marker separately and fails on zero. The `cmp`/byte-identical wording is corrected to `--verify-against` (12 significant digits, `rtol=1e-9`) everywhere it appeared, and a `feature_names_in_` ndarray migration note is in the release notes.
- Next action: Stage 3 — resolve the deprecation ledger (§3.1), then documentation and release preparation (§3.2). Keep `__version__="0.9.0a1"`.
- Blockers: Unchanged. No PyPI publication; a `v0.9.0` tag/GitHub Release stays an explicit owner action after the release PR and the asset-only workflow are merged.
- Decisive verification: All six findings reproduced on `4de1957` before the fix and confirmed after — weighted/grouped/time `"auto"` raised `ValueError` on all six probed entry points and now succeeds; the 300-ID x 2-row fixture gives `corr(enc, y) = 0.879` with `select_mrmr(k=2) -> ['id', 'weak']` (unchanged by design, now pinned), while `groups=` drives the same column to exactly zero; `fit()` reported `n_splits=5` against `fit_transform`'s 4 and now reports 4. Weighted `"auto"` matches integer row replication exactly (0.0) and the full-fit map matches sklearn's `"auto"` to 2.8e-17. Full suite green under warnings-as-errors; ruff, `git diff --check`, and a YAML parse of the workflow are clean.
