# SIFT 0.9 worklog

## Entry — 0.9.0 release PR (2026-09-02)

- Objective: Finalize the already-green 0.9 implementation as `0.9.0` in a narrow release PR, without adding features or changing behavior.
- Scope: Change `sift.__version__` and its pinned public-spine test, finalize the release-notes heading, and refresh current release-status text. Preserve historical alpha/beta and benchmark-provenance references.
- Completed: PR #73 merged to `main` as `5a582d9`; PR #74 then finalized version `0.9.0` and merged as `ec05f49`. All six required jobs passed on both release-PR head `baa117f` and the post-merge `main` commit.
- Current state: 0.9.0 release preparation is complete on synchronized `main`; no code, documentation, CI, packaging, or benchmark gate remains open.
- Next action: Stop until the owner explicitly requests the `v0.9.0` tag and GitHub Release. Only after that release should a separate commit set `0.9.1.dev0` and begin the re-scoped 0.9.1 work.
- Release boundary: Creating tag `v0.9.0` and the GitHub Release is an explicit owner action. The asset-only workflow attaches distributions and contains no PyPI publication path.
- Decisive verification: Focused public-spine, release-ledger, docs-smoke, and benchmark tests 117 passed; full warnings-as-errors suite 1,967 passed / 39 skipped with the local loky core-count guard; Ruff and `git diff --check` clean. The isolated PEP 517 build produced `sift_feature_selection-0.9.0.tar.gz` and `sift_feature_selection-0.9.0-py3-none-any.whl`; both passed `twine check`, and the exact wheel clean-installed with the latest supported dependency band and reported version `0.9.0` from outside the checkout. Both documented quick benchmark commands completed successfully. GitHub Actions run `33698372952` passed all six required jobs on exact PR head `baa117f`; post-merge run `33698657604` passed the same six jobs on `ec05f49`.

## Entry — adversarial review round on PR #73 (`stage3/0.9-docs-release`, base `7e681b8`): documentation findings (2026-09-02)

- Objective: Close the documentation half of an independent adversarial review (Grok, xhigh) of PR #73 — the eight findings against the integrated Stage 3 branch — in the status documents and the release notes. A parallel agent owns the `sift/`- and `tests/`-side fixes; this entry covers `docs/release-notes.md`, `docs/development.md`, `docs/specs/0.9-next-steps.md`, `docs/specs/0.9-product-layer.md`, `TODO.MD`, and this worklog.
- Context: Stage 3's four workstreams are integrated on `stage3/0.9-docs-release` as PR #73, CI green on all six required jobs at its head (`test (3.10)`, `test (3.11)`, `test (3.12)`, `test-catboost`, `min-pins`, `wheel-smoke`). Stages 1 and 2 closed with PR #72 (`6c6a7ea` on `main`).
- Finding 1 (P1) — **the docs overstated the docstring gate.** `docs/development.md` advertised two mechanical floors (a `Parameters` section for callables, 8 non-empty lines) while the release notes and the Stage 3 acceptance criterion promised "summary, parameters, returns, and worked examples". The resolution is to strengthen the gate to the promise, not to weaken the promise: `tests/test_docstring_coverage.py` now requires, for every `sift.__all__` export except `__version__`, a non-empty summary line, at least 8 non-empty docstring lines, a `name : type` entry for every signature parameter including `*args`/`**kwargs` (under `Parameters` for functions, under `Parameters` or `Attributes` for classes, read off `__init__`), a `Returns`/`Yields` section for functions, and an `Examples` section for every export; the new `tests/test_docstring_examples.py` parses each docstring with `doctest` and executes the `>>>` statements under warnings-as-errors. The examples test deliberately does not compare printed output — NumPy 2 scalar reprs differ across the CI matrix, so an expected-output line would pin a docstring to one interpreter — and leaves unrun only `# doctest: +SKIP` examples and CatBoost-dependent ones. Both the development guide and the release notes now also state plainly what neither test checks: documented defaults and accepted-value lists are never compared against the signature, so a stale default can stay green and remains a review responsibility.
- Finding 5 (P2) — **the "standalone" claim was an over-claim.** The release notes said each manual block "was made standalone rather than inheriting variables defined paragraphs earlier". Nine blocks carry `<!-- sift-doc: continues -->` and inherit the previous block's namespace: `DOCS.MD` ×3 (L439, L1566, L2213 — the last combined with `requires=catboost`), `docs/API.md` ×3 (L408, L522, L807), `docs/user-guide.md` L525, `docs/ADVANCED.md` L397, `docs/results.md` L46. Every one is an honest inspect-after-fit step: fit in one block, read the fitted object in the next. The notes now say standalone by default, name `continues` as reserved for those chains, name `requires=` as the catboost/matplotlib dependency gate, and record that no block is skipped without a machine-checked reason (`tests/test_docs_examples.py::test_every_skip_directive_states_a_reason` makes a reason mandatory, and the manual set currently carries no bare `skip` at all).
- Finding 4 (P2) — **two library changes had landed without a release-notes entry.** `sift/stability.py`'s `get_coef_stability` computed `coef_std / np.abs(coef_mean)` inside `np.where`, which evaluates both branches, so a feature no bootstrap ever selected divided 0 by 0 and raised numpy's invalid-value `RuntimeWarning`; it failed a `DOCS.MD` example on the Python 3.11 CI job (numpy 2.4.6) where the lasso path left a feature unselected. The ratio is now computed under `np.errstate(divide="ignore", invalid="ignore")` and the `np.where` fallback still yields `inf` (`7e681b8`). `sift/selection/view.py`'s new `_append_rows_like` (`1e9ba0f`) stops `sift.as_result` on a `KnockoffSelectionResult` with dropped inputs from tripping pandas' all-NA concat `FutureWarning`. Both are now `### Correctness` bullets — code changes, not documentation. The unused `gaussian_mi_from_corr` import dropped from `sift/selection/auto_k_xfit.py` (`9bd8c92`) is recorded alongside them.
- Finding 8 (P2) — **`_append_rows_like` changes dtypes and did not say so.** When dropped rows are appended, a NumPy `int64` or `bool` column the new rows do not carry becomes the nullable `Int64`/`boolean` dtype, because those NumPy dtypes have no missing value; float columns keep their dtype and take `NaN`, and object/extension columns are unchanged. The behavior is correct and is now documented in the release-notes bullet rather than left for a caller to discover.
- Finding 6 (P2) — **stale process text, three places.** `docs/specs/0.9-next-steps.md` claimed "the ledger check is not yet in the test suite" and carried the assertion source for whoever integrated Stage 3; the check is in the suite as `tests/test_release_notes_ledger.py` and passes, so the note and its code block are replaced by a statement of what the test asserts. `TODO.MD` still open-checked the docstring and code-block gates in one combined item; that is now split into two `[x]` items naming the four test modules, leaving one `[ ]` item — the release PR (`0.9.0a1` → `0.9.0`, retitle, pre-release smoke, merge; then the owner's `v0.9.0` tag and GitHub Release; then `0.9.1.dev0` in a separate commit). `docs/specs/0.9-product-layer.md`'s header still presented Stage 3 as the open work and repeated `be99df0` beside `6c6a7ea`; it now records Stage 3 as integrated on PR #73 pending merge, and `be99df0` survives only in §2.0, where it is the historical baseline for the two Stage-0 defects.
- Also recorded under `### Documentation`: the CatBoost row-context example in `docs/user-guide.md` now pairs `groups` with `GroupKFold` (`10b9b5c`). It previously passed `groups` to a `TimeSeriesSplit`, which ignores them and makes scikit-learn warn — a warnings-as-errors failure on the CatBoost CI job. The prose now says which splitter goes with which row context.
- Findings 2, 3 and 7 (P1 `MRMRSelector` `mrmr_backend="auto"` wording, P1 "Print progress" as the description of `verbose` on `StabilitySelector`/`BorutaSelector`/`SmartSamplerConfig`, P2 `SmartSamplerConfig.verbose` consistency) are docstring text inside `sift/` and are closed by the parallel agent; they are recorded in the Stage 3 review round in `docs/specs/0.9-next-steps.md` for completeness.
- Completed: `docs/development.md` ("Docstring standard" rewritten around the two tests and the explicit non-guarantee; the "both gates" sentence now names three); `docs/release-notes.md` (`### Documentation` docstring and code-block bullets rewritten with the real counts, a new CatBoost-example bullet; `### Correctness` gained the stability guard, the `_append_rows_like` fix with its dtype consequence, and the dropped import); `docs/specs/0.9-next-steps.md` (header, §3.2 bullets, `Stage 3 — in progress` → `integrated (PR #73)`, workstream 1 and 2 acceptance criteria, the ledger note replaced, the eight-finding review round added, `Immediate next action` rewritten); `docs/specs/0.9-product-layer.md` (rev 16 → 17, header, §6 item 6); `TODO.MD`; this worklog.
- Decisive verification: `python -m pytest tests/test_docs_examples.py -q -p no:cacheprovider` reports 122 passed / 10 skipped, and its per-file timing table gives the counts written into the release notes — 118 fenced blocks, 108 executed and 10 dependency-gated locally (README 6/0, `DOCS.MD` 48/6, `docs/API.md` 19/2, `docs/user-guide.md` 12/1, `docs/ADVANCED.md` 18/1, `docs/troubleshooting.md` 3/0, `docs/results.md` 2/0); the 14 extra passing tests are the parser and directive checks. The 122/10 split and the nine `continues` markers were cross-checked by grep against the seven manual files. `tests/test_release_notes_ledger.py`, `tests/test_release_readiness.py`, and `tests/test_docs_smoke.py` are green, so the ledger table is still byte-identical to §4. `git diff --check` clean.
- Current state: Documentation-only commit; no `sift/` or `tests/` file changed by this agent. `__version__` stays `0.9.0a1`.
- Next action: Merge PR #73, then open the release PR (`0.9.0a1` → `0.9.0`). After the owner cuts the `v0.9.0` tag and GitHub Release, bump to `0.9.1.dev0` in a separate commit and open 0.9.1 with the re-scoped documentation work (§3.2).
- Blockers: Unchanged. **No PyPI publication, ever**; the `v0.9.0` tag and GitHub Release stay explicit owner actions. The suite counts quoted in `TODO.MD` predate the new `tests/test_docstring_examples.py` and will need one refresh from a full run on the merged branch.

## Entry — Stage 3 documentation and release readiness: ledger strike, 0.9.1 re-scope, consolidated release notes (2026-09-02)

- Objective: Land the two owner decisions of 2026-09-02 in the status documents, and consolidate `docs/release-notes.md` into one truthful `0.9.0` section ahead of the release PR.
- Context: PR #72 merged to `main` as `6c6a7ea` (squash of the campaign commits, the Stage 1 corrections, and the Stage 2 CI hardening; CI green on its head `06a6e4c`). Stages 1 and 2 are closed; Stage 3 is the open work, split across seven parallel agents on disjoint files.
- Owner decision 1 — **the ledger's alias removals are struck.** The CatBoost `group_col`/`sample_weight_col` aliases and stability's `alpha` (beside `penalty`) are permanent. No `FutureWarning` is wired for them, none is planned, and no warn-and-forward test is required; a future removal would need its own full 0.9-style warning cycle first. §4 of the campaign spec now reads "unchanged — permanent alias (removal struck 2026-09-02; ...)" for both rows, and §B2's two 1.0-flip cells, §1.1's policy paragraph, §B0's implementation status, §3 (new decision #8), and §6 item 4 were brought into line. This closes §3.1 of the next-steps document by decision rather than by code — it was already one of the two options that section offered.
- Verified the wired set by grep. It is exactly two warnings: the `random_state=None` default-use `FutureWarning` via `sift/_deprecate.py::warn_random_state_none`, called at `sift/stability.py:401`, `sift/importance.py:418`, and `sift/catboost.py:804`; and the `sift.experimental` namespace `FutureWarning` at `sift/experimental.py:69`. Production tests: `tests/contracts/test_b2_conventions.py::test_none_random_state_warnings_are_caller_facing`, `tests/contracts/test_catboost_compat.py::test_catboost_none_random_state_warning_is_caller_facing`, and `tests/contracts/test_autok_ergonomics.py::test_experimental_namespace_warns_without_changing_top_level_surface` / `::test_experimental_import_forms_and_missing_name_contract`. `sift/stability.py` and `sift/catboost.py` emit no warning at all for the aliases, which is now the documented intent. `sift/_deprecate.py`'s `warn_deprecated`/`deprecated_alias` stay landed and tested (`tests/test_deprecate.py`) with no public caller in 0.9.
- Owner decision 2 — **the generated reference and its dependants move to 0.9.1.** mkdocs/mkdocstrings, retiring `docs/API.md`, folding `user-guide.md` into a tutorial, the single "which selector" decision tree, the measured runtime/scaling table with its committed script, the data-type support matrix, and the glossary are out of the 0.9.0 gate. What stays: substantive numpydoc docstrings for every public export (pinned by a coverage test over `sift.__all__`) and CI execution of every manual `python` code block. §G-docs, §5, §6 item 6, and new decision #9 in §3 record it; the campaign spec is rev 16 (2026-09-02).
- Completed: `docs/specs/0.9-product-layer.md` (rev 15 → 16, header date, §1.1, §B0, §B2, §3 decisions #8/#9, §4 ledger, §5, §G-docs, §6 items 4 and 6); `docs/specs/0.9-next-steps.md` (header, Current state, §3.1 resolved-by-decision, §3.2 re-scoped, a new `Stage 3 — in progress` block with the four workstreams and their acceptance criteria, an integration note for the ledger check, and a rewritten Immediate next action); `docs/release-notes.md` consolidated; this worklog.
- Release notes: one `## 0.9.0 (unreleased)` section — the `## 0.9.0a1` and `## 0.9.0b1` headers are gone, with one line recording that both were staged in the working tree and never published. It opens with the supersedes-0.8.0 / strictly-additive framing, then `### Breaking changes and migration` first (the old 0.9.0a1 content plus the `feature_names_in_` ndarray migration note pulled up out of the Stage 1 block), then grouped sections: sklearn contracts, leakage-safe target encoding (including the offset-numerics fix), result views, conventions, auto-k ergonomics, correctness, CI and packaging, latest-dependency compatibility, documentation, and the ledger. `## 0.8.0` is retitled "(never published; folded into 0.9.0)"; `## 0.7.0` is untouched.
- Decisive verification: A `Counter` diff of every non-blank, non-heading line between `HEAD:docs/release-notes.md` and the rewrite shows exactly fourteen dropped lines, all accounted for: the migration note that moved into Breaking changes, three repointed cross-references, and the split of one combined bullet into three (permutation-importance scorers → Conventions, CatBoost `selection_patience` → Correctness, auto-k saturation → Auto-k ergonomics). No fact was dropped. A standalone script confirms the ledger is reproduced verbatim: the 14 table lines under `### Deprecation ledger (flips in 1.0)` are byte-identical to §4's 14 lines (header, separator, 12 rows). `tests/test_release_readiness.py`, `tests/test_docs_smoke.py`, and `tests/contracts/test_public_spine.py` are green; ruff and `git diff --check` clean.
- Current state: Documentation-only commit; no `sift/` or test file changed. `__version__` stays `0.9.0a1`.
- Next action: Integrate the four Stage 3 workstreams onto one branch, run the full suite under warnings-as-errors, put it through an adversarial Grok round, then open the release PR (`0.9.0a1` → `0.9.0`) and bump to `0.9.1.dev0` after the GitHub Release.
- Blockers: Unchanged. No PyPI publication; a `v0.9.0` tag/GitHub Release stays an explicit owner action. The ledger-verbatim check is not yet in the test suite — `tests/test_release_readiness.py` has no docs-reading hook, so the assertion is written out in §3.2 of the next-steps document for whoever integrates Stage 3.

## Entry — offset-target numerics in `target_cv` `smooth="auto"` (PR #72, on top of `6017df6`)

- Objective: Fix the P1 numerical defect in the `target_cv_smoothing="auto"` empirical-Bayes shrinkage, and bring the status documents up to the current CI head.
- Latest steering: Compute every moment on `y - prior` with a stable two-pass weighted variance, in the single engine, so the out-of-fold, full-fit, weighted, grouped, and time-aware paths all inherit the fix; preserve ordinary-scale results and sklearn `"auto"` parity.
- Constraints: Unchanged — sklearn `>=1.3,<2`, existing unsafe encoders behind `allow_full_data_target_encoding=True`, fixed-k rejection of `groups`/`time`, the 58-name top-level surface, default `cat_encoding="none"`. One-hot remains blocked on F3 block-aware selection.
- Decisions: The `"auto"` shrinkage is translation *invariant* and scale *equivariant*; both are now stated contracts with tests. `lambda_i` is scale-free, so scaling `y` by `s` scales the emitted effects by exactly `s`. The explicit-float smoothing path is centered too — algebraically identical, and it removes the same cancellation for free. Binary `target_cv` is deliberately unchanged and pinned, because a 0/1 target has no offset to cancel.
- Completed: `TargetCVEncoder` accumulates its centered sums, global target variance, and within-category sums of squared deviations on `y - prior`; `_weighted_group_ssd` replaces `E[y^2] - E[y]^2` with a genuine two-pass weighted formula; `_auto_smoothed_encoding` shrinks in centered space so the final `estimate - prior` cancellation disappears; `_centered_targets` refines the weighted prior once so its own rounding error does not leak into the effects. Sixteen tests added. Status documents updated: `docs/specs/0.9-next-steps.md` (CI head `6017df6`, release-workflow language, three adversarial-review rounds), `docs/specs/0.9-product-layer.md` (rev 15, E correction landed, numerics note in §E.1), `docs/release-notes.md`, and this worklog.
- Current state: Stages 1 and 2 are complete. PR #72 (`stage1/0.9-corrections`) was green at `6017df6` on `test (3.10)`, `test (3.11)`, `test (3.12)`, `test-catboost`, `min-pins`, and `wheel-smoke`. This commit sits on top of that head and has not yet had its own CI run. The asset-only GitHub-release workflow is on the default branch since PR #71 (`d10afef`), so no release event can publish to PyPI. Stage 3 (deprecation ledger, then documentation and release preparation) is the open work.
- Next action: Stage 3 — resolve the deprecation ledger (§3.1), then documentation and release preparation (§3.2). Keep `__version__="0.9.0a1"`.
- Blockers: Unchanged. No PyPI publication; a `v0.9.0` tag/GitHub Release stays an explicit owner action after the release PR merges.
- Decisive verification: Reproduced before the fix on a 300-row / 6-level regression fixture. `fit_transform(X, y)` vs `fit_transform(X, y + 1e8)`, max difference in the centered OOF encoding: 0.185 plain, 0.076 integer-weighted, 0.112 float-weighted, 0.170 grouped, 0.306 time-aware; 0.051 in the target-blind `transform`. On two near-tie designs the error decided the selection: `select_mrmr(k=1)` flipped `['cat'] -> ['numeric']` (encoding difference 0.157) and `select_cefsplus(k=1)` flipped `['numeric'] -> ['cat']` (0.230). An explicit `smooth=5.0` differed by only 4.4e-8 at the same offset, which localized the defect to the empirical-Bayes path. After the fix: <=5.6e-16 against the target the shifted run actually sees in all five contexts, <=1.9e-9 against raw `y` (float64 resolves only ~1.5e-8 near 1e8, so `y + 1e8` cannot carry `y` more exactly), and both selector flips are gone. Twelve of the sixteen new tests fail against the pre-fix encoder; the four that pass by design are the three scale-equivariance guards and the binary pin. sklearn `smooth="auto"` parity holds at 8.9e-16 over twelve seeds. Full suite 1,713 passed / 25 skipped under warnings-as-errors (1,697/25 before, +16); ruff and `git diff --check` clean.

## Entry — adversarial review rounds on PR #72 (`stage1/0.9-corrections`, base `4de1957`)

- Objective: Close the six findings of an independent adversarial review (Grok) of PR #72, and record Stage 1/Stage 2 completion honestly.
- Latest steering: Fix every finding; do not change selection numerics for finding 2 — fix the over-claim instead.
- Completed: Round 1 (`020e082`) closed all six findings and marked Stages 1 and 2 complete in `docs/specs/0.9-next-steps.md` with the PR #72 CI evidence (test 3.10/3.11/3.12, test-catboost, min-pins, wheel-smoke all green) and the four CI-only fixes that were needed (full-history checkout for the gate-provenance test, the nested auto-k stub keyword, CatBoost seed warnings, platform-stable gate rendering). Round 2 (`6017df6`) closed three verification leftovers: the `docs/API.md` sentence still requiring an explicit smoothing float on weighted/grouped/time-aware `target_cv`, a near-unique boundary test that could not fail, and a workflow comment claiming the whole matrix installs the NumPy 2.5.2 band.
- Decisions: `target_cv_smoothing="auto"` is valid on the weighted, grouped, and time-aware paths — the empirical-Bayes prior is the unweighted formula with counts replaced by weighted row mass, so it was always well defined there and the gate was simply wrong. The only genuinely undefined case, a fitting slice with no positive weight mass, still raises for both `"auto"` and an explicit float, and is pinned. Near-unique identifiers keep their numerics: a level appearing twice in training legitimately carries its sibling row's target, so this is documented as the boundary of the centering guarantee (drop ID-like columns, or pass `groups=`) rather than engineered away.
- Decisive verification: All six round-1 findings reproduced on `4de1957` before the fix and confirmed after — weighted/grouped/time `"auto"` raised `ValueError` on all six probed entry points and now succeeds; the 300-ID x 2-row fixture gives `corr(enc, y) = 0.879` with `select_mrmr(k=2) -> ['id', 'weak']` (unchanged by design, now pinned), while `groups=` drives the same column to exactly zero; `fit()` reported `n_splits=5` against `fit_transform`'s 4 and now reports 4. Weighted `"auto"` matches integer row replication exactly (0.0) and the full-fit map matches sklearn's `"auto"` to 2.8e-17. Full suite green under warnings-as-errors; ruff, `git diff --check`, and a YAML parse of the workflow clean.

## Entry — Stage 1 planning, pre-CI (historical)

Retained for the record; superseded by the entries above.

- Objective: Repair the verified 0.9 correctness/contract gaps, obtain the campaign's first CI evidence, and reach an honest no-PyPI 0.9.0 release-ready state.
- Decisions: `target_cv` must emit fold-centered effects on training and inference paths so unique levels are neutral rather than fold markers. New target-derived encoding is rejected for knockoffs because it invalidates the Model-X claim. Honest `input_kind="unknown"` and the row-preserving warmup-policy alias remain; large module splits wait until after correctness and CI.
- Completed: Workstream D committed at `8263e00`; E1 at `63e32c3`; E2 added weighted, grouped, and temporal `target_cv` encoding across function filters, nested auto-k evaluation, selector wrappers, binary CEFS+, Boruta, and knockoffs, with target-blind inference.
- Decisive verification at the time: stronger probes reproduced the fold-prior leak, the duck-array sklearn crash in both supported environments, stray target-CV metadata keys, the DataFrame `feature_names_in_` type failure, the Stability view ordering mismatch, missing Auto-K curves, and missing knockoff raw width. Those failures defined Stage 1's gate, which is now closed.
