# SIFT audit corrections

## Objective and authority

Active goal: address all agreed issues in Fable's post-roadmap audit of d038698. User explicitly requested the goal and implementation on 2026-09-07 and has now approved the local clean-source commit and existing runtime-benchmark refresh to finish it. Earlier delivery PR78-93 remains merged; this is a correction round. Branch codex/0.9.1-audit-corrections from clean d038698.

Preserve unrelated changes and 0.9.1.dev0. Local commits and the existing runtime refresh are explicitly authorized; no push, merge, release, tag, PyPI publication, old-PR/PID cleanup or other external action. Deliver a reviewed, tested correction ready for integration. Preserve documented raw-unit scoring, typed category identity/repr ordering, ordinary bootstrap and FDP definitions unless separately justified and authorized.

## Workflow and state

Same Grok writer session6942c6e1-14da-42e0-8ef2-27e77a0ab942, workspace/medium, now quota-exhausted; same Opus reviewer72bc11aa-3fc5-4cae-8b5a-9197b89f270c, read-only/xhigh. User explicitly requested continuation. Codex completes unfinished implementation and independently verifies; if Opus quota fails, Codex continues directly. No extra review without a demonstrated issue. Later-stage disposition handoff: /private/tmp/sift-audit-remaining-dispositions.md. All native triage lanes completed; panel/proxy and knockoff lanes completed bounded Stage2 implementation follow-ups. Root owns integration.

Read installed grok/claude-cli skills before launch. Exact python3 ~/.codex/skills/... launcher spellings. Managed sandbox now requires require_escalated for provider private state and protected .git/.agents writes. Task-relevant transmission remains authorized; exclude secrets and unrelated repos. Never bypass denial. Prompts under /private/tmp mode600, remove after terminal. Poll exact handles; observation timeout is not termination. Grok alone writes implementation while active; Codex owns worklog/integration.

Audit root: /private/tmp/claude-501/-Users-konstantinmedvedovsky-code-sift/ada31870-965d-4a55-a2cd-2d85b369f489/scratchpad. Report sift-0.9.x-roadmap-audit.md. Inspect scripts before use; some prepend audit copies to sys.path. Ensure live-source imports; no blind artifact writes/full-suite executions.

## Stages and acceptance

1. Wrong results/routing: compare prefix identity and purged-split time forwarding; materialize conditioning iterables once; preserve elbow block units after dropped cache columns; sanitize/reject callable nonfinite importances; reject duplicate categorical labels before width expansion; cap rolling max_train_size after purge/embargo; route weighted multi-target auto-k only to supported methods and reject unsupported dense checks clearly. Minimal public regressions.
2. Composition/validity: reject frozen caches under Stabilized resampling; prevent duplicate observations crossing wrapped inner CV using safe weighted dedupe where supported or explicit restriction; warn/restrict unseen-dimension within-validation interpretation without fitting validation target effects. Fix nested supervised-encoding knockoff downgrades and floating minimum-q boundary. Triage concrete remaining panel/proxy/knockoff claims.
3. Manifests: content identity for long configurations; opt-in target/weight/group/time fingerprints without retained training data; truthful resolved randomness, ModelSelector seed, intact Stabilized base snapshot and multi-target effective metadata; deterministic supported object/label tokens or explicit unsupported errors. Preserve private/optional hashing and honest unavailable legacy provenance; no new general provenance system.
4. Accepted smaller API/docs items: check repros before changing contracts. Update release entries/counts/evidence link, remove process chatter, reconcile matrix/spec status; clarify raw-unit scoring, ordinal/type identities, within estimand and proxy-edge/onehot limits. Preserve frozen bakeoff CSV/FDP arithmetic; disclose discovery floor/nonselection rates and retract unsupported CEFS+ recommendations.
5. Focused regression and independent repro acceptance, then full warnings-as-errors suite, Ruff, API/matrix generators, strict docs/diff check. Runtime binding becomes stale after source edits: resolve clean-source refresh at an authorized implementation checkpoint, never present dirty-source results as release-grade. No completion until all agreed issues and decisive verification are done.

## Qualified/rejected claims

- Multi-target evaluate scale sensitivity follows documented raw-unit RMSE; do not silently standardize.
- Repr-order ordinal codes and int/float identity distinction are explicit current policy, not accidental regressions. Clarify limits; no unapproved reorder/coercion.
- Unseen entity/time fallback is future-safe but cannot establish within-entity validation; warn/restrict interpretation, never use validation target means.
- Empty-selection FDP=0 is correct. Improve discovery-rate reporting, not arithmetic.
- Dense-check failures already warn and record diagnostics; improve early capability handling, not a claim of silence.
- Redundancy reports deliberately exclude selected-selected candidates while clustering includes their edges: reconcile overbroad docs, not silently redefine API.
- Future allowlist/stale process text is docs work, not proof of invalid mathematics. Remaining P2/P3 inventory is not automatically accepted.

## Existing evidence and commands

Previous-turn independent repros confirmed wrong compare prefixes, missing split time, empty capped purged folds, NaN importance winning, bootstrap inner-CV optimism/frozen-cache mismatch, generator exclusion loss, block-curve units, duplicate label3to5 expansion, unsupported weighted 2-D auto route, manifest collisions/lost seeds, nested FDR and49-selection infeasibility warning. Original2409passed40skipped/six CI jobs are historical, not correction acceptance.

TestPython /private/tmp/sift-pytest9.5nI4QH/bin/python; base /opt/anaconda3/bin/python; docs /private/tmp/sift-docs-venv/bin/python. OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 LOKY_MAX_CPU_COUNT=8. No dependency installs without authority. No concurrent tests/reviewers during an eventual timed refresh.

Frozen quality benchmarks/results/knockoff_statistic_bakeoff.csv SHA25640d4e7944b81b012996f9c9f08327b1c7f2be33a4eee766f9af7a0a482c88acf, sourceae904b8af02037eb66cd649384c4665dba17049d: do not regenerate. Historical runtime source3c6f9d1b3777211be86832c1b7e6f58b466faeaf CSVsha7f0c09743415337baf55eabe2be6a019ad26a4ce956e3fd3c0561dd344de5785.

Stage1 accepted: Grok116/117 terminal0; Codex200focused-Werror/Ruff/diff and independent block/generator/purged/optional-time/no-routed-work probes pass. Opus98 found optional-time regression; Opus99 terminal0 unified32737 with154focused checks/two probe batches confirms both corrected issues closed. Summary /private/tmp/sift-audit-stage1-review-summary.md. Accepted files are staged only as the next review baseline, not committed.

Stage2 accepted: Grok118 terminated exit1 with HTTP402 balance exhaustion; no retry/reset/purchase. Codex and both native writers completed the accepted scope. Opus100 read-only/xhigh terminal0 unified78663 found one KSG weight-capability/guidance gap; Codex corrected it and added bootstrap/block restriction plus successful-half regressions. Codex198focused-Werror before this delta;187focused-Werror after (13Stage1 tests excluded,2KSG tests added), full Ruff/diffcheck and literal49-boundary/nested-FDR oracles pass. All writers/reviewer terminal; caller provider prompt removed. Summary and protocol notes: /private/tmp/sift-audit-stage2-review-summary.md. No additional review for the tiny closed capability gap. Stage2 becomes the next staged baseline; no commit. Frozen quality CSV hash remains40d4e794... with no artifact writes.

Stage3+4 source/docs complete and reviewed. Native writers terminal. Manifests now provide typed ordered long-config digests, streamed deterministic object hashes, opt-in caller context hashes with honest completeness/unverified-selection-time flags, compact cache provenance without training arrays, legacy delegates and effective multi-target metadata. StabilitySelector records replayable None-seed roots; Stabilized derives only unset declared base seeds, preserving explicit seeds and row draws. Numeric onehot no-op, conditioned-auto preflight, compare unused val_frac/actual empty-design behavior, and accepted docs/spec/matrix/benchmark-interpretation corrections are implemented. Raw-unit metrics, typed ordinal policy, future1.0 owner decisions and frozen quality artifacts remain intact.

Opus101 read-only/xhigh terminal0 unified18611 (same native session) ran29focused checks plus independent probes. One finding: compare() unconditionally hashed otherwise valid unsupported labels. Codex reproduced Decimal failures in both modes, added an unavailable-hash fallback plus both public regressions. Full-suite checkpoint2475passed40skipped4failed additionally exposed one new duck-array len(X) regression (two tests), a within-composition test retaining the now-rejected group_cv setup, and the expected stale runtime fingerprint. Fixed shape-safe original row counting; changed only the composition test to overlapping kfold, preserving the new unseen-level guard. Post-delta165passed2optional-skipped; full Ruff/diffcheck pass. No extra review for these narrow fixes. Summary/protocol: /private/tmp/sift-audit-stage34-review-summary.md; frozen packet /private/tmp/sift-audit-stage34-review.tNZDEW. Provider caller prompt removed, all provider/native actors terminal.

Decisive earlier Stage3 probes: cache privacy/context completeness/opaque seeds/cross-process frozenset hashes pass. Same2000x10 string fixture keeps exact digest and reduces median tracemalloc peak5,970,868B to164,267B (not RSS/runtime); before/after artifacts in /private/tmp/sift-audit-hash-memory-{before,after}.json. API generator66exports, data-type matrix--check, strict MkDocs, and docs ledger checks pass. Quality CSV40d4e794... and provenance JSONbd84c19ca731cfb92e553c308c74969f547484355448b676fbad89a0d0606cd9 unchanged.

Current state: final full warnings-as-errors suite terminal1 (unified8045),2480passed40skipped1failed in82.46s; all executable docs pass. Sole failure is the known runtime source-hash binding. All implementation accepted and staged. User explicitly approved a local commit and refresh of the existing18-case benchmark; goal active again. Live process check found no active pytest or benchmark worker; long-lived idle Python/Claude services and normal desktop activity remain. Next: commit reviewed corrections locally, confirm clean source, run unchanged18cases with1warmup/7timed calls, update actual runtime CSV/provenance/documented table, verify the remaining binding gate, and retain the refreshed evidence locally. Preserve historical quality CSV/JSON and all benchmark parameters. No extra review or optimization round; no push/merge/tag/release/publication.
