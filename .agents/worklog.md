# SIFT 0.9.1 closure — local work ready for Astra review

Objective: deliver the reviewed closure fixes. The owner authorized a push, PR, and merge commit after required CI passes on the exact PR head. A version bump, tag, GitHub Release and publication remain out of scope; the owner still chooses 0.9.1 versus 0.10.0.

State: main and origin/main remain clean at c72900c. Local integration branch audit/0.9.1-closure-fixes preserves Fable's CI provenance fix and all seven closure branches as merge commits. Astra's bounded correction commit 4dee900 reports actual `within_two_way_iterations`, makes the partial-unseen warning and public docs precise, and removes stale runtime/hash claims. The final evidence commit refreshes the 18-case runtime CSV, sidecar, canonical table and checksum from clean source 4dee900; sidecar `dirty=false`, 86 source hashes, one warmup/seven repetitions, and all 18 data/selection fingerprints match the prior evidence. Frozen knockoff bakeoff checksum is unchanged.

Decisive verification: integrated suite before Astra review 2760 passed, 41 skipped, one runtime-binding test deselected pending evidence. Astra correction slice 154 passed. After refresh, runtime binding plus panel suite 68 passed; Ruff, diff check and strict MkDocs passed. Prior compare A/B matched c72900c on 11 unchanged routes and four feature-path routes; classification stratification was the intended difference. CatBoost optional environment: 75 marked tests and 41 adapter/view tests passed. Fable's fresh-clone CI provenance test passed. The additional wrapper skip is an intentional one-hot incompatibility case.

Next: push the clean integration branch, open and attach a PR, wait for the six required jobs on its exact head, then merge with a merge commit. Verify the tested tree, fast-forward clean primary main, dispatch the scheduled workflow once and confirm benchmark-smoke and test-latest-deps. Repair only concrete failures. No local implementation remains pending absent a CI finding.
