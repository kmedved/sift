# Python 3.13 CI support — in progress

Objective: add Python 3.13 to SIFT CI, fix demonstrated compatibility failures, and update current support docs/metadata. Deliver a scoped green PR for Astra review before merge. No 1.0 API/default changes, version bump, tag, release, or PyPI action.

State: PR #100 first head found one Python 3.13-only failure in generated dataclass equality on a shared pandas DataFrame; commit 63ad499 restores the earlier tuple-comparison behavior. Runtime evidence was refreshed from that clean source commit; all 18 data and selection fingerprints match and only `sift/selection/knockoff_filter.py` changed among bound sources. Repaired head 44e8f2e passed all six active PR jobs in run 35549323358, including Python 3.13.15 at 2,792 passed / 34 skipped with NumPy 2.5.3, pandas 3.0.6, scikit-learn 1.9.1, SciPy 1.18.1, Numba 0.67.0. Current README, development guide, unreleased notes, and package classifier now reflect this observed support. Focused selection-view/runtime checks passed 179/179, and the edited docs/release-note/runtime slice passed 9/9. Local MkDocs cannot load `pymdownx` in the Anaconda environment; the existing Python 3.12 CI job installs docs extras and runs strict MkDocs.

Next: commit/push documentation and classifier changes, read all required checks including 3.13 and strict MkDocs on the exact final head, then callback to Astra with PR #100 for review. Do not merge before Astra review. Preserve runtime and frozen bakeoff artifacts.
