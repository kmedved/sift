<!-- Generated by scripts/generate_data_type_matrix.py; do not edit. -->
# Data-Type Support

This page is an observed public-behavior matrix, not a planned-capability
chart. Each cell is a seeded, bounded baseline call of a public selector
entry point. Numeric, sparse, and row-metadata axes use `n=32` rows and
4 feature columns; the categorical fixture adds a string column (`p=5`);
the datetime/timedelta fixture adds `when` and `elapsed` (`p=6`).
Iteration, draw, and runtime knobs are reduced so the probes stay cheap;
algorithm-routing and data-type acceptance/encoding defaults are left in
place, with two explicit exceptions: stability uses a fixed `alpha=1.0`
instead of automatic alpha search, and CatBoost uses
`algorithm="prediction"` with `prefilter_k=None` instead of its SHAP
and prefilter defaults. Warnings are captured for classification and
are not copied into the table. The probes never change selector
mathematics or public defaults to make a cell succeed.
SIFT-specific terms are defined in the [glossary](glossary.md).

Regenerate with:

```bash
python scripts/generate_data_type_matrix.py
```

## Legend

| code | status | meaning |
| --- | --- | --- |
| `yes` | supported | the seeded, bounded baseline call completed |
| `no` | rejected | the baseline call raised |
| `cond` | conditional | the baseline call raised, and one documented enabling option succeeded |
| `dep` | dependency-gated | installing the optional extra can unlock this axis |

Enabling options used for `cond` cells:

- categorical filters/Boruta: `cat_encoding="target_cv"` (in-library; no extra)
- `KnockoffSelector` categorical: `cat_encoding="loo_logit"` (warns; `fdr_control='none'`)
- groups/time: `k="auto"` with `AutoKConfig(k_method="evaluate")` and the matching split strategy

CatBoost cells publish the known post-install contract in every
environment: `no` for ndarray, sparse, and datetime/timedelta; `dep`
for DataFrame, categorical, weights, groups, and time. CatBoost-marked
tests pin those post-install outcomes and do not rewrite the page.

## Matrix

<!-- data-type-matrix:start -->
| entry | ndarray | DataFrame | categorical | sparse | datetime/timedelta | weights | groups | time |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `select_mrmr` | yes | yes | cond | no | no | yes | cond | cond |
| `select_jmi` | yes | yes | cond | no | no | yes | cond | cond |
| `select_jmim` | yes | yes | cond | no | no | yes | cond | cond |
| `select_cefsplus` | yes | yes | cond | no | no | yes | cond | cond |
| `select_cefsplus_binary` | yes | yes | cond | no | no | yes | cond | cond |
| `MRMRSelector` | yes | yes | cond | no | no | yes | cond | cond |
| `JMISelector` | yes | yes | cond | no | no | yes | cond | cond |
| `JMIMSelector` | yes | yes | cond | no | no | yes | cond | cond |
| `CEFSPlusSelector` | yes | yes | cond | no | no | yes | cond | cond |
| `CEFSPlusBinarySelector` | yes | yes | cond | no | no | yes | cond | cond |
| `select_fdr` | yes | yes | no | no | no | yes | no | no |
| `KnockoffSelector` | yes | yes | cond | no | no | yes | no | no |
| `select_cached` | yes | yes | no | no | no | yes | no | no |
| `select_boruta` | yes | yes | cond | no | no | yes | yes | yes |
| `BorutaSelector` | yes | yes | cond | no | no | yes | yes | yes |
| `select_boruta_shap` | dep | dep | dep | no | no | dep | dep | dep |
| `StabilitySelector` | yes | yes | no | no | no | yes | yes | yes |
| `stability_regression` | yes | yes | no | no | no | yes | yes | yes |
| `stability_classif` | yes | yes | no | no | no | yes | yes | yes |
| `permutation_importance` | yes | yes | yes | no | yes | yes | yes | yes |
| `smart_sample` | no | yes | no | no | yes | no | yes | yes |
| `catboost_select` | no | dep | dep | no | no | dep | dep | dep |
| `catboost_regression` | no | dep | dep | no | no | dep | dep | dep |
| `catboost_classif` | no | dep | dep | no | no | dep | dep | dep |
<!-- data-type-matrix:end -->

## Observed non-baseline cells

Numeric ndarray/DataFrame successes and ordinary weight successes are
omitted here; they are the `yes` cells in the matrix. Everything else is
listed with a stable public-contract description. Raw exception type and
message text is kept on the probe attempt for diagnostics and is not
copied onto this page.

<!-- data-type-notes:start -->
| entry | axis | status | observed behavior |
| --- | --- | --- | --- |
| `select_mrmr` | categorical | conditional | baseline `cat_encoding="none"` rejects string/category columns; `cat_encoding="target_cv"` encodes them without extras |
| `select_mrmr` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `select_mrmr` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `select_mrmr` | groups | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `groups` splits is accepted |
| `select_mrmr` | time | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `time` splits is accepted |
| `select_jmi` | categorical | conditional | baseline `cat_encoding="none"` rejects string/category columns; `cat_encoding="target_cv"` encodes them without extras |
| `select_jmi` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `select_jmi` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `select_jmi` | groups | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `groups` splits is accepted |
| `select_jmi` | time | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `time` splits is accepted |
| `select_jmim` | categorical | conditional | baseline `cat_encoding="none"` rejects string/category columns; `cat_encoding="target_cv"` encodes them without extras |
| `select_jmim` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `select_jmim` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `select_jmim` | groups | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `groups` splits is accepted |
| `select_jmim` | time | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `time` splits is accepted |
| `select_cefsplus` | categorical | conditional | baseline `cat_encoding="none"` rejects string/category columns; `cat_encoding="target_cv"` encodes them without extras |
| `select_cefsplus` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `select_cefsplus` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `select_cefsplus` | groups | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `groups` splits is accepted |
| `select_cefsplus` | time | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `time` splits is accepted |
| `select_cefsplus_binary` | categorical | conditional | baseline `cat_encoding="none"` rejects string/category columns; `cat_encoding="target_cv"` encodes them without extras |
| `select_cefsplus_binary` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `select_cefsplus_binary` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `select_cefsplus_binary` | groups | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `groups` splits is accepted |
| `select_cefsplus_binary` | time | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `time` splits is accepted |
| `MRMRSelector` | categorical | conditional | baseline `cat_encoding="none"` rejects string/category columns; `cat_encoding="target_cv"` encodes them without extras |
| `MRMRSelector` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `MRMRSelector` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `MRMRSelector` | groups | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `groups` splits is accepted |
| `MRMRSelector` | time | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `time` splits is accepted |
| `JMISelector` | categorical | conditional | baseline `cat_encoding="none"` rejects string/category columns; `cat_encoding="target_cv"` encodes them without extras |
| `JMISelector` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `JMISelector` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `JMISelector` | groups | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `groups` splits is accepted |
| `JMISelector` | time | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `time` splits is accepted |
| `JMIMSelector` | categorical | conditional | baseline `cat_encoding="none"` rejects string/category columns; `cat_encoding="target_cv"` encodes them without extras |
| `JMIMSelector` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `JMIMSelector` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `JMIMSelector` | groups | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `groups` splits is accepted |
| `JMIMSelector` | time | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `time` splits is accepted |
| `CEFSPlusSelector` | categorical | conditional | baseline `cat_encoding="none"` rejects string/category columns; `cat_encoding="target_cv"` encodes them without extras |
| `CEFSPlusSelector` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `CEFSPlusSelector` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `CEFSPlusSelector` | groups | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `groups` splits is accepted |
| `CEFSPlusSelector` | time | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `time` splits is accepted |
| `CEFSPlusBinarySelector` | categorical | conditional | baseline `cat_encoding="none"` rejects string/category columns; `cat_encoding="target_cv"` encodes them without extras |
| `CEFSPlusBinarySelector` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `CEFSPlusBinarySelector` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `CEFSPlusBinarySelector` | groups | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `groups` splits is accepted |
| `CEFSPlusBinarySelector` | time | conditional | fixed-k baseline calls reject row context; `k="auto"` with evaluate `time` splits is accepted |
| `select_fdr` | categorical | rejected | non-numeric columns are rejected; encode categoricals before the Gaussian estimator |
| `select_fdr` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `select_fdr` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `select_fdr` | groups | rejected | the public function has no `groups` argument |
| `select_fdr` | time | rejected | the public function has no `time` argument |
| `KnockoffSelector` | categorical | conditional | baseline `cat_encoding="none"` rejects string/category columns; `cat_encoding="loo_logit"` completes with a UserWarning and `fdr_control='none'` because supervised encoding drops the Model-X claim |
| `KnockoffSelector` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `KnockoffSelector` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `KnockoffSelector` | groups | rejected | row groups are rejected; use `feature_groups` for grouped discoveries |
| `KnockoffSelector` | time | rejected | time-aware fitting is rejected |
| `select_cached` | categorical | rejected | non-numeric columns are rejected; encode categoricals before the Gaussian estimator |
| `select_cached` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `select_cached` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `select_cached` | weights | supported | weights belong on `build_cache(X, sample_weight=w, subsample=None)`; `select_cached` has no call-time `sample_weight` because the cache already stores row weights |
| `select_cached` | groups | rejected | `select_cached` has no groups or time argument; row context is not part of the cache-backed selector |
| `select_cached` | time | rejected | `select_cached` has no groups or time argument; row context is not part of the cache-backed selector |
| `select_boruta` | categorical | conditional | baseline `cat_encoding="none"` rejects string/category columns; `cat_encoding="target_cv"` encodes them without extras |
| `select_boruta` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `select_boruta` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `select_boruta` | groups | supported | `groups` is accepted on the seeded baseline call |
| `select_boruta` | time | supported | `time` is accepted on the seeded baseline call |
| `BorutaSelector` | categorical | conditional | baseline `cat_encoding="none"` rejects string/category columns; `cat_encoding="target_cv"` encodes them without extras |
| `BorutaSelector` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `BorutaSelector` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `BorutaSelector` | groups | supported | `groups` is accepted on the seeded baseline call |
| `BorutaSelector` | time | supported | `time` is accepted on the seeded baseline call |
| `select_boruta_shap` | ndarray | dependency-gated | the default SHAP backend needs CatBoost; without CatBoost, an explicit non-CatBoost estimator still requires the optional `shap` package |
| `select_boruta_shap` | DataFrame | dependency-gated | the default SHAP backend needs CatBoost; without CatBoost, an explicit non-CatBoost estimator still requires the optional `shap` package |
| `select_boruta_shap` | categorical | dependency-gated | baseline `cat_encoding="none"` rejects string/category columns; `cat_encoding="target_cv"` is required, and the SHAP backend then needs CatBoost or the `shap` package with an explicit compatible estimator |
| `select_boruta_shap` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `select_boruta_shap` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `select_boruta_shap` | weights | dependency-gated | the default SHAP backend needs CatBoost; without CatBoost, an explicit non-CatBoost estimator still requires the optional `shap` package |
| `select_boruta_shap` | groups | dependency-gated | the default SHAP backend needs CatBoost; without CatBoost, an explicit non-CatBoost estimator still requires the optional `shap` package |
| `select_boruta_shap` | time | dependency-gated | the default SHAP backend needs CatBoost; without CatBoost, an explicit non-CatBoost estimator still requires the optional `shap` package |
| `StabilitySelector` | categorical | rejected | string/category columns are not accepted as numeric features |
| `StabilitySelector` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `StabilitySelector` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `StabilitySelector` | groups | supported | `groups` is accepted on the seeded baseline call |
| `StabilitySelector` | time | supported | `time` is accepted on the seeded baseline call |
| `stability_regression` | categorical | rejected | string/category columns are not accepted as numeric features |
| `stability_regression` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `stability_regression` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `stability_regression` | groups | supported | `groups` is accepted on the seeded baseline call |
| `stability_regression` | time | supported | `time` is accepted on the seeded baseline call |
| `stability_classif` | categorical | rejected | string/category columns are not accepted as numeric features |
| `stability_classif` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `stability_classif` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `stability_classif` | groups | supported | `groups` is accepted on the seeded baseline call |
| `stability_classif` | time | supported | `time` is accepted on the seeded baseline call |
| `permutation_importance` | categorical | supported | `permutation_importance` forwards X to the fitted model; with a dtype-tolerant fitted estimator the public call completes. Support is model-dependent |
| `permutation_importance` | sparse | rejected | sparse matrices are rejected independently of the fitted model; X must be a 2D dense array or pandas DataFrame |
| `permutation_importance` | datetime/timedelta | supported | `permutation_importance` forwards X to the fitted model; with a dtype-tolerant fitted estimator the public call completes. Support is model-dependent |
| `permutation_importance` | groups | supported | `groups` is accepted on the seeded baseline call |
| `permutation_importance` | time | supported | `time` is accepted on the seeded baseline call |
| `smart_sample` | ndarray | rejected | requires a pandas DataFrame; ndarray input is rejected |
| `smart_sample` | categorical | rejected | string/category columns are not accepted as numeric features |
| `smart_sample` | sparse | rejected | requires a pandas DataFrame; sparse input is rejected |
| `smart_sample` | datetime/timedelta | supported | the baseline call completes after coercing datetime/timedelta feature columns to epoch-scale float32 values, which can be lossy; convert them to numeric features explicitly |
| `smart_sample` | weights | rejected | there is no input `sample_weight` argument; the sampler writes its own `sample_weight` column |
| `smart_sample` | groups | supported | `groups` is accepted as `group_col` on `SmartSamplerConfig`, not as a `groups=` array argument |
| `smart_sample` | time | supported | `time` is accepted as `time_col` on `SmartSamplerConfig`, not as a `time=` array argument |
| `catboost_select` | ndarray | rejected | ndarray input is rejected; pass a pandas DataFrame |
| `catboost_select` | DataFrame | dependency-gated | requires the optional `catboost` extra; installing it unlocks this axis |
| `catboost_select` | categorical | dependency-gated | requires the optional `catboost` extra; installing it unlocks this axis |
| `catboost_select` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `catboost_select` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `catboost_select` | weights | dependency-gated | requires the optional `catboost` extra; installing it unlocks this axis |
| `catboost_select` | groups | dependency-gated | requires the optional `catboost` extra; installing it unlocks this axis |
| `catboost_select` | time | dependency-gated | requires the optional `catboost` extra; installing it unlocks this axis |
| `catboost_regression` | ndarray | rejected | ndarray input is rejected; pass a pandas DataFrame |
| `catboost_regression` | DataFrame | dependency-gated | requires the optional `catboost` extra; installing it unlocks this axis |
| `catboost_regression` | categorical | dependency-gated | requires the optional `catboost` extra; installing it unlocks this axis |
| `catboost_regression` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `catboost_regression` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `catboost_regression` | weights | dependency-gated | requires the optional `catboost` extra; installing it unlocks this axis |
| `catboost_regression` | groups | dependency-gated | requires the optional `catboost` extra; installing it unlocks this axis |
| `catboost_regression` | time | dependency-gated | requires the optional `catboost` extra; installing it unlocks this axis |
| `catboost_classif` | ndarray | rejected | ndarray input is rejected; pass a pandas DataFrame |
| `catboost_classif` | DataFrame | dependency-gated | requires the optional `catboost` extra; installing it unlocks this axis |
| `catboost_classif` | categorical | dependency-gated | requires the optional `catboost` extra; installing it unlocks this axis |
| `catboost_classif` | sparse | rejected | sparse input is rejected; pass a dense NumPy array or pandas DataFrame |
| `catboost_classif` | datetime/timedelta | rejected | datetime/timedelta feature columns are rejected; convert them to numeric features explicitly |
| `catboost_classif` | weights | dependency-gated | requires the optional `catboost` extra; installing it unlocks this axis |
| `catboost_classif` | groups | dependency-gated | requires the optional `catboost` extra; installing it unlocks this axis |
| `catboost_classif` | time | dependency-gated | requires the optional `catboost` extra; installing it unlocks this axis |
<!-- data-type-notes:end -->

## How to read the axes

- **ndarray / DataFrame.** Dense numeric features. Named DataFrame columns
  are the usual production path; positional ndarrays get generated `x0...`
  names.
- **categorical.** A string column beside numeric columns, with default
  `cat_encoding`. `target_cv` is the in-library enabling option used for
  `cond` cells. Filter selectors also accept `cat_encoding="onehot"`
  (atomic `{column}__{level}` blocks; not a matrix enabling option and
  rejected on knockoffs/Boruta). `loo_logit` is also in-library (binary).
  `loo` / `target` /
  `james_stein` need `.[categorical]`. Function-style filters and all
  Boruta entry points (including `BorutaSelector`) additionally require
  `allow_full_data_target_encoding=True` for those legacy supervised
  modes; sklearn filter selector classes and `KnockoffSelector` do not.
  That extra path is not a matrix column.
- **sparse.** SciPy CSR input. Function and class routes are probed on
  the same CSR axis. Low-level rejection messages differ; this matrix
  normalizes them to one user-facing status and note.
- **datetime/timedelta.** Feature columns, not row metadata. Convert them
  to numeric features explicitly if they belong in the candidate set.
- **weights.** `sample_weight=` on the public call, except `select_cached`,
  whose weights are supplied to `build_cache` and stored on the cache.
- **groups / time.** Row metadata arrays (or, for `smart_sample`, config
  column names). A datetime `time=` array is metadata, not a feature.
  Fixed-k filter functions and classes reject this context; automatic-k
  evaluate splits accept it.

## Coverage limits

- Fixtures are tiny and seeded. Completing a call is not a quality,
  FDR, or runtime claim.
- `permutation_importance` is probed with a fitted dtype-tolerant estimator
  so categorical and datetime cells exercise SIFT rather than a numeric
  model's `fit`. Support remains model-dependent. Sparse input is rejected
  by SIFT independently of that estimator.
- `select_cached` is probed as `build_cache` plus `select_cached`. Weights
  are passed to `build_cache`. Axes that are not arguments of
  `select_cached` fail at the public call.
- `smart_sample` reduces rows, not columns. Ndarray and sparse probes call
  it with that object directly; the public function is documented to take
  a DataFrame.
- CatBoost ndarray, sparse, and datetime/timedelta cells are `no`.
  Datetime/timedelta columns are not CatBoost feature types; convert
  them to numeric features explicitly. DataFrame, categorical, weights,
  groups, and time are `dep`. CatBoost-marked tests pin the post-install
  outcomes. `select_boruta_shap` needs CatBoost for its default estimator;
  without CatBoost an explicit non-CatBoost estimator still needs the
  optional `shap` package, and categorical input still needs `target_cv`
  before that SHAP check.
- Low-level path/stopping-rule helpers (`select_k_*`, curve builders) and
  standalone `build_cache` are outside this selector-entry-point matrix.
  `sample_knockoffs` is a public generator, not a selector, and is out of
  scope. `select_cached` is intentionally probed as `build_cache` plus
  `select_cached`.
- Multiclass helpers, Polars frames, sparse *output*, one-hot encoding,
  metadata-alias axes, and the 0.9.x F-workstream APIs are out of scope.
- Complex dtypes, duplicate column labels, and empty `feature_names`
  remain the sklearn-contract tests' job.
