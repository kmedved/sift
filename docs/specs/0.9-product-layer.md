# SIFT 0.9 — the product-layer campaign

Status: active implementation, rev 9 (Stage 0 and 0.9.0a1 complete; dated
clean-commit D9 evidence recorded).
Baseline: `main` at `be99df0` + the Stage-0 fixes described in §2.0.

## 1. Why 0.9 exists

The 0.8.0 line was a correctness campaign: four hardening rounds closed the silent-failure paths,
the statistically-backwards defaults, the cache contracts, and the packaging blockers. What it
deliberately did not touch is the *product layer*. Today:

- **55 top-level exports**, including auto-k helpers the docs themselves call experimental or
  failed-gate. A new user cannot tell the load-bearing 12 names from the research surface.
- **5 incompatible result types** (`FilterSelectionResult`, `KnockoffSelectionResult`,
  `BorutaResult`, `CatBoostSelectionResult`, `FeaturePathEvaluationResult`) plus two entry points
  (`StabilitySelector`, `permutation_importance`) with no result object at all.
  `selected_features` is an attribute on three of them and a *method* on `BorutaResult`;
  `StabilitySelector.selected_features_` holds indices where the other classes hold names.
- **49 `AutoKConfig` fields × 14 `k_method`s**, with overlapping-but-distinct pairs a user must
  disambiguate (`n_splits` = evaluate folds vs `xfit_folds` = fold-curve folds; `elbow_patience`
  vs `stop_patience` govern different stops; `objective_n_eff` overlaps `n_eff_mode=<float>` for
  the penalized objective only) and one accepted configuration value that is currently
  reserved and unimplemented (`xfit_mode="exact"`; both public and low-level xfit paths reject it).
- **Convention drift**: `groups=` arrays vs `group_col=` names vs columns-inside-`X` (CatBoost);
  `random_state` 0 / 42 / None; `n_jobs` 1 / −1; `verbose=True` with **43 `print(` sites** and one
  `logging` user; `alpha` means an L1 penalty in one module and a significance level in two others;
  `select_cached` has four positional return shapes.
- sklearn integration stops at `get_feature_names_out`: no `SelectorMixin` semantics,
  `transform` emits columns in *selection* order, no `inverse_transform`, and no metadata-routing
  requests.
- The categorical story is "raise with a good message": there is still no in-library, leakage-safe
  path for regression + string columns.

0.9 is the release where the algorithms stay put and the *interface* catches up, organized around
one mental model:

> Every selector is "rank candidates → build an ordered path → apply a stopping rule → return a
> subset". Knockoffs, Boruta, and stability are "score every feature → threshold". The API should
> say so.

**Non-goals for 0.9**: new estimators of MI, changes to the copula/log-det/knockoff math, formal
Meinshausen–Bühlmann error control for stability, GPU work, polars-native internals.

### 1.1 Compatibility policy (the campaign's hard constraint)

**0.9 is strictly additive.** Concretely:

- Every 0.8 call — same arguments, same defaults — produces the same selections, the same return
  types, the same output ordering, and every existing export. `__all__` grows (55 existing names
  plus the three 0.9.0 additions — **58** total in 0.9.0); later 0.9.x additions are explicitly
  counted or module-scoped. Nothing is removed, no defaults change, no output order changes, no
  CatBoost `k` semantics change in 0.9.
- New behavior arrives only as **new options** (`output_order=`, `return_result=`, presets, option
  groups) or **new warnings**. Anything slated to flip does so in **1.0**, after a full 0.9 cycle
  of `FutureWarning`s emitted by a new `sift._deprecate.warn_deprecated(name, since="0.9",
  removal="1.0")` helper (the repo currently has zero deprecation machinery).
- The 1.0 flips are enumerated **now**, in §4 (the deprecation ledger), so "what changes in 1.0"
  is a table, not a surprise. This resolves rev 1's contradiction between "every 0.8 call works"
  and its own default changes: those changes are all 1.0 items.
- Enforcement is mechanical, not aspirational: workstream B0 builds a **compatibility contract
  matrix** (parametrized tests over method × route × input type × options; §2.B0) plus golden
  selection snapshots, and every later PR in the campaign must keep it green.

---

## 2. Workstreams

Sizes: S ≈ a day, M ≈ 2–4 days, L ≈ a week+ of focused time including tests and docs. Rev 1
under-sized B and G; both are now split. Order within each workstream is implementation order.

### 2.0 Stage 0 — close the outstanding defects before any snapshot is recorded (S) — **done**

Two defects existed on `main` at `be99df0` and are fixed alongside this rev (with regression
tests in `tests/test_release_readiness.py`); contract
snapshots must be recorded only on top of these:

1. `StabilitySelector`'s DataFrame `transform` contract was incomplete: missing or renamed
   selected columns raised a raw pandas `KeyError`, and a selector fitted on a positional ndarray
   raised `KeyError: "None of [Index(['x0'...])..."` when handed a DataFrame. Now: name-based
   selection still tolerates extra/reordered columns (pinned by test), missing/renamed columns
   raise a clear `ValueError`, and positional-fit + DataFrame input to `transform` or
   `tune_threshold` raises a clear `ValueError` directing the caller to pass an ndarray or refit
   on the DataFrame. Both methods share duplicate, missing-column, and generated-name guards.
2. The `auto_dense_*` router-field stripping missed two paths that rebuild configs: the Gaussian
   late **fallback** (built from the *original* config) and the **binary** auto router. Both now
   route through `_strip_router_only_fields`.

Subsequent review rounds surfaced five follow-ups, also closed and tested:

3. Stripping alone made binary log-loss `auto_dense_check` **silently ignored** (no log-loss
   dense-regime diagnostic exists). Binary log-loss CEFS+ automatic routing now **rejects**
   non-default `auto_dense_*` options with a `ValueError` naming them, instead of ignoring or
   mis-warning; default configs route cleanly with no unused-field noise. Binary Brier selection
   delegates to Gaussian CEFS+ and keeps the Gaussian dense-check behavior.
4. Duplicate DataFrame column labels corrupted `StabilitySelector` output width (selecting `a`
   from columns `a, a, b` returned both copies). Duplicate columns are now rejected at fit and at
   transform, and duplicate explicit `feature_names` are rejected.
5. `feature_names=[]` was treated as "absent" by preprocessing (`or`-falsy) but as "explicit" by
   the positional-input flag. `is None` is now the single absence test; an explicit empty list is
   rejected up front by the non-empty validation.
6. The Gaussian-fallback regression test exercised only the helper; it now drives the real
   `select_gaussian_auto_path` fallback dispatch (monkeypatched runner) and asserts the second
   dispatched config carries default `auto_dense_*` fields.

7. The smart-sampler path validated an explicit `feature_names` sequence and then silently widened
   it back to every numeric DataFrame column. It now preserves the requested subset and order,
   rejects scalar or unordered containers and nonnumeric explicit names clearly, and carries the
   resolved names into fitted metadata. Datetime and timedelta feature columns are rejected by
   the numeric feature-preprocessing guard; a configured datetime `time` metadata column remains
   valid metadata and is excluded from the feature set. Fold-local threshold tuning retains that
   metadata without widening the fitted subset. Exact tuple/MultiIndex and missing-value label
   identity is preserved, and unhashable labels fail validation. Stage-0 regression tests pin
   those contracts, including the exclusion of explicitly named group/time metadata columns.

### B0. Compatibility contract matrix + deprecation infrastructure (M)

Replaces rev 1's "three golden datasets", which cannot establish "every 0.8-style call".

- A parametrized **contract matrix** over: every public entry point × ndarray/DataFrame input ×
  {no weights, weights} × {no cache, cache} (where supported) × {default, explicit} settings ×
  {no groups/time, groups, time, both} (where supported) × categorical presence × optional-dep
  availability (catboost / category_encoders gates). Each cell asserts: selection, return **type**,
  output ordering, warning set (kind and count), and metadata keys. Cells that cannot run in the
  base environment are collected and asserted in the CatBoost CI job.
- Golden **selection snapshots** on a handful of fixed datasets sit on top as a cheap smoke layer,
  not as the compatibility proof.
- `sift._deprecate` helper + a test utility asserting "alias X warns once and forwards to Y".
- A deterministic, committed **Auto-K gate summarizer**: the G1–G6 gate table
  (`benchmarks/results/auto_k_v2_gates.csv`) is currently not reproducible from the repo; ship the
  script that computes it from the raw CSVs, with a unit test on a fixture.

**Acceptance.** The matrix passes on `main`; any behavior-affecting diff turns a cell red; the
gate table can be regenerated byte-identically.

**Implementation status.** B0 is complete at the route-supported contract level. The compatibility
spine pins the ordered legacy 55-name public prefix plus the additive `set_verbosity`, and a coverage
inventory maps every public export to a substantive behavioral test. Parametrized cells cover
fixed-k functions and selector wrappers, every `select_cached` method/tuple shape and omitted
defaults, named/positional inputs, weighted regimes, Auto-K group/time contexts, categorical
functions/classes, smart sampling, `select_fdr`, CatBoost, stability, Boruta, knockoffs, path
evaluation, and permutation importance. Unsupported combinations have explicit rejection cells;
optional category-encoder and CatBoost cells are collected in their dependency-enabled CI jobs.
Selections, exact result identities, ordering, warning signatures, fitted metadata, cache
contracts, and omitted-versus-explicit defaults are pinned. The internal `sift._deprecate` helper
and exact warn-and-forward tests are also complete.

The deterministic gate summarizer has strict raw-schema/provenance validation and a byte-identical
fixture test. A dedicated D9 runner now measures the missing complete fixed-k `select_cached` path
denominator and writes a checksum-bound provenance sidecar. The historical July gate remains
irrecoverable because its denominator was not recorded and its `k_oracle` aggregation convention
was mixed; it is never overwritten. The clean `88a8705` run is recorded as
`auto_k_v2_d9_fixed_k_path_2026-08-31.csv` and
`auto_k_v2_d9_fixed_k_path_2026-08-31.provenance.json`: both D9 seeds use the full 50k-by-2000
design, their median path times are 0.1373203751 s and 0.1353520839 s, and provenance reports
`dirty=false`. `auto_k_v2_gates_mean_oracle_2026-08-31.csv` is the dated canonical mean-oracle
recomputation. Its path-only G5 ratios are explicitly cross-run evidence combining the legacy
method rows with the fresh denominator, not a reconstruction of the missing July timing.

### B1. Logging and warning cleanup (M) — no selection/return/default changes

- All 43 `print(` sites route through `logging.getLogger("sift")`; `verbose=True` (unchanged
  default in 0.9) maps to an INFO-level handler so today's output still appears. This preserves
  selection, return values, defaults, and whether the default call emits progress, but does not
  promise byte-identical formatting, logger routing, or output interleaving.
- `sift.set_verbosity("info"|"debug"|None)` convenience; long loops (bootstraps, Boruta
  iterations, CatBoost splits, greedy paths) accept `callback=fn(step, total, info)`.
- Warning audit: every `warnings.warn` gets an explicit category and `stacklevel`; messages report
  *effective* values (`top_m`, `n_used`), never configured ones. Existing warning kinds/counts stay
  unchanged except for the new, enumerated compatibility/deprecation warnings in B2 and §4.

**Implementation status.** B1 is complete. All 43 direct progress prints now emit INFO records
through `logging.getLogger("sift")`, `set_verbosity` is additive, and every package warning has an
explicit category and caller-facing stack level. The existing warning kinds/counts are preserved,
and the CEFS+ saturation warning names its effective path depth. Additive callbacks cover Stability
bootstraps, Boruta iterations, CatBoost splits, direct `select_cached`, fixed and Auto-K filter
dispatch, all filter selector classes, classic/Gaussian mRMR and JMI/JMIM, Gaussian CEFS+, and
binary CEFS+ path steps. `callback=None` retains the original kernels; randomized and near-collinear
parity tests pin the opt-in compiled bridges.

### B2. Conventions — additions in 0.9, flips in 1.0 (M)

0.9 adds the target conventions beside the old ones; §4 lists the 1.0 flips.

| concept | 0.9 (additive) | 1.0 (flip) |
| --- | --- | --- |
| row metadata | `groups="colname"` / `time="colname"` DataFrame sugar accepted wherever the corresponding arrays are supported; CatBoost gains `groups=`/`time=`/`sample_weight=` arrays beside `group_col`/`sample_weight_col` | `*_col` params deprecated-removed |
| `random_state` | `None`-default sites (stability, permutation, CatBoost) warn when the default is used; `42`-default sites (`AutoKConfig`, `SmartSamplerConfig`, `evaluate_feature_path`) retain the literal `42` default in 0.9 (omission equals explicit `42`, with no sentinel or omission warning) | defaults become `0` everywhere |
| `n_jobs` | unchanged; documented per entry point | default `1` everywhere |
| `verbose` | unchanged (`True`), now logging-backed | default `False` |
| name collisions | stability gains `penalty=` alias for its `alpha`; CatBoost `catboost_params` keys that collide with translated kwargs emit a **`UserWarning`** (today: silent override) | collisions become errors; old names removed |
| scoring | sklearn scorer objects accepted by `StabilitySelector.tune_threshold`, `evaluate_feature_path`, and auto-k `metric=` (permutation importance already does); sift short names stay | — |
| `select_cached` | `return_result=True` added (returns the A-protocol view); tuple forms unchanged | tuple forms deprecated |
| `permutation_importance` | keeps returning a DataFrame (it is a function, not an estimator); `return_result=True` opts into an `ImportanceResult` carrying the per-repeat matrix for CIs | — |

### A. `SelectionResult` as a **protocol + adapters**, not a replacement (L)

Rev 1's inheritance plan is withdrawn: forcing five frozen dataclasses with different fields into
one hierarchy would break constructors, pickles, and `isinstance` checks, and the
`selected_features` attribute cannot coexist with Boruta's `selected_features()` method on one
class. Instead:

- **`SelectionView`** (a small concrete class) with the unified surface, and a
  `sift.as_result(obj, input_features=None)` adapter — plus a `.result_view()` method on each
  existing result class and a `result_view_` property on `StabilitySelector` — that wraps, never
  replaces. The view has two explicit identities whenever preprocessing expands or encodes
  columns: `raw_input` (`n_raw_features`, raw names, raw `columns_hash`) and `encoded_output`
  (`n_encoded_features`, encoded names, encoded `columns_hash`, and a raw-to-encoded mapping).
  `raw_table` is one row per caller-supplied column; `encoded_table` is one row per fitted encoded
  column when that representation is available. `support_`/`indices` refer only to the raw input
  width, while `encoded_support_`/`encoded_indices` refer only to the encoded output width; no
  single mask is promised to represent both widths. Not every legacy object can reconstruct either
  full identity (e.g. `CatBoostSelectionResult` does not carry the pre-prefilter column list), so
  incomplete adapters set the corresponding fields to `None` and
  `metadata["table_complete"] = False`. Workstream A ships a per-adapter completeness matrix as
  documentation and tests:

```python
view = sift.as_result(anything_sift_returned_or_fitted)
view.features      # ordered selected names — the ONE canonical accessor (new name, no collision)
view.indices       # positions in the caller's X, or None
view.support_      # boolean mask over input columns
view.encoded_support_  # mask over encoded output columns, or None
view.k
view.table         # alias for raw_table: one row per raw input column
view.encoded_table # one row per encoded output column, or None
view.curve         # k, criterion, criterion_se, selected — same names for every auto-k route
view.metadata      # effective params only; includes schema_version
view.diagnostics
view.proxies(feature, r_min=0.8)
view.plot(ax=None)         # lazy matplotlib
view.to_dict()             # JSON-safe, versioned
view.transform(X)          # column subset; order per output_order (default preserves today's)
view.inverse_transform(X_selected)  # requires retained fitted encoder state
```

- **All existing classes, constructors, defaults, pickle formats, and `isinstance` relations are
  unchanged in 0.9.** `FilterSelectionResult` keeps being returned by `return_result=True`;
  Boruta's `selected_features()` stays a method; `StabilitySelector.selected_features_` stays
  indices for every input type. The view is where unification lives.
- Under the hood, adapters delegate to what each selector already computes; where a value does not
  exist (e.g. Boruta has no per-step gain), the column is **absent**, never NaN-padded.

`table` schema: `feature, selected_index, path_rank, selected, relevance, gain,
redundancy_at_selection, reason_dropped` with `reason_dropped ∈ {None, "constant", "top_m",
"corr_prune", "zero_weight_variance", "nonpositive_relevance", "not_reached"}`. This is the
schema for `raw_table`, one row per caller-supplied column. `encoded_table` is separately defined
over encoded output columns and carries `raw_feature`/`block_id` mapping columns; it is not
substituted for `raw_table`. A fitted view may transform raw input through its retained encoder
and inverse-transform selected encoded output. A result-only adapter that lacks fitted encoder
state must set `metadata["transform_available"] = False` and raise a clear `NotImplementedError`
for `transform`/`inverse_transform`; it may still expose names, tables, and metadata. Block
selections additionally expose `block_id` and member columns in `diagnostics`. Per selector,
`gain` is the log-det step gain (CEFS+), JMI increment, score-test gain (binary), `W` (knockoffs),
mean importance (Boruta), mean |coef| (stability), score delta (CatBoost).
`features` is the selected raw-feature accessor; encoded names are exposed separately as
`encoded_features` when an encoded representation exists.

**Contract details the view must specify (rev-1 gaps, now normative):**

1. *Duplicate names*: `raw_table` carries `selected_index`; name-keyed accessors raise on ambiguity
   with a message pointing at positional access.
2. *Provenance*: `metadata["input_kind"] ∈ {"dataframe", "positional"}` and separate
   `metadata["raw_columns_hash"]`/`metadata["encoded_columns_hash"]`; a view built from a cache
   records cache provenance (`n_rows_original`, `feature_names_are_synthetic`).
3. *Incomplete rankings*: `metadata["table_complete"] = False` whenever any raw or encoded
   column has no row-level information (e.g. legacy adapters); `plot()`/`proxies()` degrade
   explicitly.
4. *`transform` validation*: same rules as the fitted classes — named inputs validated by name,
   positional by width; positional-fit + DataFrame input raises (mirrors the Stage-0 stability
   fix and the 0.8 cache contract). Encoding-aware transform and inverse-transform additionally
   require the fitted encoder state to have been retained; metadata marks result-only adapters
   without that state as unavailable.
5. *Serialization*: `to_dict()` embeds `schema_version` (starts at `"1"`); consumers must ignore
   unknown keys; DataFrames serialize as `orient="split"`.
6. *Memory*: `proxies()` works from stored **correlations**, never retained `X`: only an explicit
   `store_proxies=True` opts into storing the selected×candidate float32 block at selection time;
   there is no hidden automatic capture based on block size. When not requested,
   `proxies()` raises with the exact option to pass. The stored block has a documented memory cap
   (default ≤ 64 MB), and requests exceeding it fail explicitly rather than silently retaining
   `X` or truncating the block.

**Acceptance.** `sift.as_result(x)` works on the output/fitted state of all seven entry-point
families; one "Reading results" doc page runs the same five lines against each; the contract
matrix shows zero changes to legacy returns.

### C. `k="auto"` ergonomics: presets, option groups, staged surface trim (M)

- **Presets** (constructors mapping to today's semantics, no new methods):
  `AutoKConfig.default()` (router), `.predictive(strategy="kfold", rule="best", n_folds=5)`
  (gaussian_cv), `.discovery(alpha=0.05)` (chi2_stop, min_k=0), `.downstream(strategy, metric,
  rule)` (evaluate).
- **Option groups** (`objective=`, `test=`, `perm=`, `knockoff=`, `cv=`, `stability=`,
  `experimental=`) are ergonomic builders and read-only views, not additional dataclass fields.
  `AutoKConfig` keeps its existing flat fields as the sole canonical storage, and its explicit flat
  constructor continues to accept every 0.8 kwarg throughout 0.9. A documented
  `AutoKConfig.from_groups(...)` builder (plus the presets above) validates group objects and
  immediately writes their values into those flat fields; `cfg.objective`, etc. are immutable
  views synthesized from the flat fields. Group objects are not accepted alongside flat kwargs in
  the same constructor, so there is no duplicate-storage or precedence ambiguity. Unknown or
  conflicting fields passed to the builder fail before construction. `dataclasses.replace`, `==`,
  `repr`, explicit flat kwargs, and pickle round-trips operate on the unchanged flat dataclass
  fields only; group views do not appear as stored fields or alter those semantics. The overlapping
  fields (`n_splits`/`xfit_folds`, `elbow_patience`/`stop_patience`, `objective_n_eff`/`n_eff_mode`,
  `xfit_mode`) are **not** deprecated as duplicates: they are semantically distinct today. C ships
  a field-semantics audit table first; any consolidation is a 1.0 item with warn-and-forward
  mapping only where the audit proves equivalence. Unused-field validation completes its coverage
  and runs only on the user's config (never routed copies — generalizing the Stage-0 fix).
- **Surface trim, staged**: `sift.experimental` is created and documented in 0.9.0, while
  `sift.__all__` keeps all 55 legacy names and adds exactly `as_result`, `SelectionView`, and
  `set_verbosity` (**58 names in 0.9.0**); later 0.9.x additions must be counted explicitly in
  their release notes or kept module-scoped and never silently folded into the 58 count. The
  experimental names emit a `FutureWarning`
  on import-time attribute access via module `__getattr__`. The exact 1.0 allowlist (replacing
  rev 1's "≈20") is fixed now:

  `__version__`, `select_mrmr`, `select_jmi`, `select_jmim`, `select_cefsplus`,
  `select_cefsplus_binary`, `select_fdr`, `sample_knockoffs`, `build_cache`, `select_cached`,
  `FeatureCache`, `AutoKConfig`, `FilterSelectionResult`, `KnockoffSelectionResult`,
  `evaluate_feature_path`, `MRMRSelector`, `JMISelector`, `JMIMSelector`, `CEFSPlusSelector`,
  `CEFSPlusBinarySelector`, `KnockoffSelector`, `StabilitySelector`, `stability_regression`,
  `stability_classif`, `BorutaSelector`, `BorutaResult`, `select_boruta`, `select_boruta_shap`,
  `permutation_importance`, `smart_sample`, `SmartSamplerConfig`, `panel_config`,
  `cross_section_config`, `catboost_select`, `catboost_regression`, `catboost_classif`,
  plus `select_k_auto`, `select_k_elbow`, `select_k_penalized_objective` (which stay public),
  plus 0.9 additions `as_result`, `SelectionView`, `set_verbosity` — **42 names**, with
  workstream-F additions (`sift.compare`, `Stabilized`, `ModelSelector`,
  `PurgedTimeSeriesSplit`/`GroupPurgedTimeSeriesSplit`) joining the allowlist as they land.
  Everything else moves under `sift.experimental` (auto-k research helpers: the remaining
  `select_k_*`, `bootstrap_paths`, `null_objective_paths`, `path_gain_pvalues`,
  `gaussian_cv_curves`, `xfit_objective_curves`, `compute_objective_for_path`,
  and `FeaturePathEvaluationResult`, whose 1.0 move is definite) in 1.0. `ImportanceResult` and
  the option-group dataclasses are module-only types and do not consume top-level export slots.
  The 42-name allowlist is the fixed 1.0 base; any F-workstream additions are counted by name when
  they land, rather than inferred from the 0.9.0 total.

### D. sklearn integration, corrected (M)

1. **Version reality**: the floor is `scikit-learn>=1.3,<2`, and `cross_validate(...,
   params={...})` plus stable metadata routing are 1.4+/1.6+ features. All routing work and its
   acceptance tests are **version-gated** (`pytest.importorskip` on
   `sklearn.utils.metadata_routing` / version checks); the floor stays 1.3 for 0.9. Raising the
   floor to 1.4 is a 1.0 decision recorded in §4.
2. Per-selector metadata requests for the arguments that selector actually accepts (for example,
   `set_fit_request(sample_weight=True, groups=True, time=True)` on a group/time-aware auto-k
   selector) so, on sklearn ≥1.4 with routing enabled, `cross_validate(pipe, X, y,
   params={"groups": g})` reaches the selector. A selector must not advertise a request for
   metadata its fit path rejects. On 1.3 the documented path remains passing fit kwargs directly.
3. **Internal-estimator audit**: the evaluate path's `RidgeCV`/`LogisticRegression`, stability's
   `GridSearchCV`, and `tune_threshold`'s pipeline are audited for weight/group plumbing (do
   internal CV splitters and scorers see the weights the outer call received?), with findings
   fixed or documented. This is the substantive gap behind rev 1's one-line routing bullet.
4. **`SelectorMixin` semantics, not a drop-in**: adopt `_get_support_mask` and gain
   `inverse_transform`; keep the custom DataFrame handling and the sparse rejection message. A new
   `output_order="legacy"|"original"` parameter defaults to `"legacy"` in 0.9, where "legacy" is
   **explicitly fixed per selector family**: filter paths emit path/selection order; Boruta emits
   original input order; `StabilitySelector` emits features in descending selection frequency,
   with stable original-index order for ties, regardless of whether `threshold` alone governs or
   `max_features` caps the set. The default flips to `"original"` everywhere in 1.0 (ledger §4).
   Each class's current sklearn `check_estimator`/equivalence status is pinned by the contract
   matrix rather than asserted here; known deviations carry their own documented causes (e.g.
   `KnockoffSelector`'s seeded-noise row-order sensitivity, tagged `non_deterministic`).
5. Tags audit **per selector** (not a global `allow_nan=True`): classic/Gaussian filters impute →
   `allow_nan=True`; binary CEFS+ and stability document their own NaN behavior and tag
   accordingly; `non_deterministic` only where true (KnockoffSelector today).
6. CI pins a per-class list of green `check_estimator` checks (with the documented deviations
   skipped explicitly), so contract regressions are caught by the suite.

### E. Categorical story: leakage-safe by construction (M)

1. **Reuse sklearn's cross-fitted `TargetEncoder` (available since 1.3)** as the engine for
   `cat_encoding="target_cv"` in the unweighted, ungrouped case: internal-CV `fit_transform` for
   selection, target-blind `transform` for inference — exactly the selector-class semantics.
   Implement custom out-of-fold splitting **only** where sklearn's encoder cannot go: sample
   weights, `groups` (GroupKFold folds), and `time` (forward-chained folds).
2. Normative details rev 1 left open: fold-local m-estimate smoothing (`smooth="auto"` delegating
   to sklearn where used; explicit `loo_smoothing`-style float otherwise); unknown categories at
   transform → global target mean; missing values → own category; and a stated guarantee: with
   `groups`/`time` provided, no encoded value is computed from its own row's fold, group, or
   future. Time-aware folds are fully specified: rows in the earliest fold have no history and
   **must not** receive a full-data target mean or any other target-derived prior. They receive an
   explicitly user-supplied, target-independent `target_prior`, or the caller must select a
   `warmup_policy="exclude"|"zero_weight"` (the latter is the default) that excludes those rows
   from the selection fit. Tied timestamps are never split across a fold boundary and are encoded
   only from strictly earlier times; at inference `transform` applies the encoder fitted on all
   training rows, so unseen or future times are ordinary rows for it. Encoding metadata uses one
   schema in every route: `encoding_cv.kind ∈ {"fixed_k", "group", "time"}` and
   `encoding_cv.n_splits` is the effective fold count. Fixed-k selector classes and fixed-k
   function APIs both report `kind="fixed_k"` and continue to reject `groups`/`time` (as in 0.8);
   group/time-aware auto-k selector paths may report `group`/`time` when those metadata inputs are
   accepted. No fixed-k path claims a grouped/time encoding guarantee.
3. Unsupervised fallbacks that never leak: `"ordinal"`, `"frequency"`, `"onehot"`
   (`onehot_max_levels` cap, remainder pooled).
4. **Ordering constraint (accepted from review): minimal block support (F3) lands before one-hot
   encoding ships as a selector option**, so a category enters/leaves as a unit from day one.
5. The `allow_full_data_target_encoding=True` escape hatch and `category_encoders` backends stay,
   documented as the expert path.

**Acceptance.** `select_mrmr(df_with_strings, y, k=10, task="regression",
cat_encoding="target_cv")` works with no extras installed; a high-cardinality-ID leakage test
shows the ID column not selected under `target_cv` but selected under the unsafe opt-in; the
grouped/time guarantee has a direct test.

### F. New capabilities (independent; ordering constraints from review applied)

**F1. Conditioning set — `include=`, `exclude=`, `candidates=` (S/M).** Exact under the log-det
objective: condition the partial-Cholesky state on `include` at step 0, then run the greedy path;
`exclude`/`candidates` filter the pool. Review prototype: a 40-line reference reproduced sift's
CEFS+ path exactly, then showed the conditional top-3 given the 8 raw housing features
(`MedInc*Latitude` gain 0.026, …) differs from the unconditional top-8, which re-discovers four
already-included features. Extends naturally to classic paths. For `select_fdr`, conditioning is
FDR-compatible only when the `include` set is **prespecified or chosen on data independent of the
knockoff run** (sample-split); metadata records the conditioning provenance
(`include_provenance ∈ {"prespecified", "sample_split", "data_derived"}`) and `"data_derived"`
runs are labeled exploratory with no FDR statement.

**F2. Proxy / redundancy report (S).** `view.proxies("MedInc", r_min=0.8)` lists unselected
columns with |copula-r| ≥ threshold against each selected feature, computed from the
selected×candidate correlation block **stored at selection time** (never by retaining `X`; see
A-contract 6). Cluster-level frequencies for stability outputs: in 30 half-sample reruns `MedInc`
appeared in 40% of paths only because it trades places with `MedInc*Longitude` (copula r = −0.999);
the cluster frequency is 100%. Docs get the corollary: monotone transforms are *identical* under
the rank-Gaussian copula.

**F3. Block/group-aware selection (M/L) — prerequisite for one-hot (E4).** `feature_blocks=`
(explicit dict or `"auto"` from one-hot prefixes): block gain = log-det ratio of the block given S
(few-column Cholesky update); classic paths use max-over-block relevance with block-level
redundancy. Owner decision (accepted): **`k` counts blocks**, `metadata["n_columns_selected"]`
reports raw width. Knockoffs' cluster-representative grouping gets a `feature_blocks` alias.

**F4. `Stabilized(selector)` meta-selector (M) — after F8b.** Generalizes stability from
Lasso-only to any selector: `Stabilized(CEFSPlusSelector(k=20), n_resamples=30,
resample="half"|"bootstrap"|"blocks", threshold=0.6)` → per-feature (and, via F2,
per-cluster) selection frequencies. Row metadata is supplied at fit/routing time, not stored as
constructor options: `stabilized.fit(X, y, groups=g, time=t)` (or explicitly request those
metadata through the sklearn routing API). Sequencing per review: the knockoff e-value
aggregation (F8b) is validated first, then offered as `Stabilized`'s derandomization rule for
`KnockoffSelector` bases; `StabilitySelector` and the auto-k `stability` route become thin presets
in 1.0, unchanged in 0.9.

**F5. Multi-target CEFS+ (M).** For `Y` (n×q): greedy gain of feature j is
`−½·log(1 − c_jᵀ Σ_{Y|S}^{-1} c_j / var(x_j|S))`, `c_j = cov(Y, x_j | S)` — a rank-1 update,
O(q²) per candidate per step on the existing cache (whose docstring has promised multi-target
selection since 0.5). API: 2-D `y` auto-detected by `select_cefsplus`/`select_cached`;
`evaluate` scores multi-output ridge. Two design notes must be settled in-workstream before any
auto-k default: (i) `Σ_{Y|S}` is solved with the same shrinkage/ridge guard as the feature side
and collinear targets (condition number above a stated cap) are rejected with guidance to drop or
combine them; (ii) the information criterion's degrees of freedom (`q·k` vs per-target EBIC
summation) is derived and validated on synthetic multi-target designs — it is not assumed.

**F6. Generic `ModelSelector` (L).** `catboost_select`'s RFE/forward/stability machinery with any
sklearn estimator and group/time-aware CV, plus `PurgedTimeSeriesSplit` /
`GroupPurgedTimeSeriesSplit`. `catboost_select` becomes the CatBoost preset with an unchanged
public signature. Adds an opt-in **nested** scoring mode so the reported score of the chosen k is
not the fold score that chose it (today's known optimism).

**F7. Panel within/between selection (S/M).** `within="groups"` applies the weighted within
transform before the rank transform: per-entity **weighted** means (using `sample_weight`) are
subtracted from X and y; `within="two_way"` alternates entity and time demeaning for a fixed,
documented iteration count. In every CV context (auto-k evaluate/fold curves, `sift.compare`),
demeaning parameters are **fit on training folds only** and applied to validation rows;
entities unseen in training fall back to the global training mean. `table` gains
`within_relevance`/`between_relevance` columns.

**F8. Knockoff UX (S/M).** (a) `min_feasible_q` and "would select N at offset=0" metadata plus a
warning scoped to knockoff+ (`offset=1`) when `m·q < 1`, where `m` is the **effective** tested
count (post-screening; group-level when grouped) — not raw `p`. (b) **Opt-in**
  `aggregation="evalues"` for `n_draws > 1`, defaults unchanged (Ren & Barber 2022:
  `e_j = m·1{W_j ≥ T_q}/(1 + #{W ≤ −T_q})` per draw). e-BH runs over exactly the common tested
  universe `T`, with `m = |T|`; `m` is not the number of nonzero e-values. A feature screened out
  in draw `d` remains in `T` and contributes `e_j=0` for that draw, rather than being dropped or
  diluting a per-draw denominator. If screening sets vary by draw, `T` is their union and every
  draw uses this same `m`; the sets and zero padding are recorded in metadata. A varying union that
  was not fixed before the knockoff statistics, or a screening rule without the required
  swap-invariance proof, is exploratory and receives no FDR statement. Validated ungrouped mode is
  restricted to shipped statistics whose sign-flip antisymmetry is proved and regression-tested;
  no statistic inherits that status merely by being shipped. Under a validated swap-invariant
  screening rule, **stated precisely**: the FDR guarantee is conditional on Model-X
  exchangeability holding for the plug-in Gaussian-copula knockoff draws — i.e. it inherits the
  library's existing `approximate_plugin` framing, it does not upgrade it; metadata says so.
  Grouped e-value mode is exploratory/no-FDR until a valid group statistic and corresponding proof
  exist. (c) The **statistic bakeoff** the 0.8 docs promised: a committed,
seeded benchmark (independent / AR(1) / block-correlated / dense-weak × {relevance, lsm, ridge,
cefsplus}) with provenance recorded, whose results either justify flipping the default to `ridge`
in 1.0 or keep `relevance` with numbers in the repo. (The review-time measurement — power
0.39 vs 0.90 on AR(1) at equal cost — is currently uncommitted scratch evidence and is treated as
a hypothesis for this benchmark, not as a committed result.)

**F9. `sift.compare` (M) — leakage-safe by design, before manifests.** Rev 1's version (score
precomputed selections by CV on the same data) would leak: a subset selected using all of `y`
must not be evaluated by resampling that same data. Redesign:

```python
sift.compare(
    {"cefs": lambda: CEFSPlusSelector(k="auto"), "fdr": lambda: KnockoffSelector(q=0.1)},
    X, y, estimator=Ridge(), cv=GroupKFold(5), groups=g,
)   # refits SELECTION inside every training fold; scores on the untouched fold
```

Selector *factories*, per-fold selection refit, per-fold downstream fit/score; report score
distribution, mean k, and selection-frequency overlap across folds. A cheap
`mode="in_sample_path"` variant (prefix scoring of a full-data path) exists but is labeled
in-sample in every output. `view.to_dict()`-based **reproducibility manifests** land after this,
reusing its fold bookkeeping. Manifest contents are enumerated now: sift/numpy/pandas/
scikit-learn/scipy/numba versions, BLAS runtimes via `threadpoolctl.threadpool_info()`, git
commit when available, `X` shape + column-name hash (+ opt-in data hash), cache provenance
(`n_rows_original`, synthetic-names flag), the *effective* configuration, seeds, and per-fold
split fingerprints (hashes of index arrays).

### G. Docs, packaging, and CI debt — split honestly (was one "M"; now three)

**G-docs (L).**
1. One canonical manual: `DOCS.MD` narrative + generated API reference from numpydoc docstrings
   (mkdocs/mkdocstrings); writing real docstrings for the public surface is the bulk of this work
   (`help(select_mrmr)` is one line today). `docs/API.md` retired once generation is live;
   `user-guide.md` folds into a tutorial; `ALGORITHMS.md` stays as the math page; one decision
   tree replaces the five "which selector" tables.
2. Every fenced `python` block in the manual executed in CI (extending the 0.8 README-block test),
   with shared fixtures so examples are **standalone** — several today are API-valid but depend on
   variables defined paragraphs earlier (e.g. the user-guide time-holdout example defines
   `timestamps` but inherits `X`/`y`), which is exactly what fixture-backed execution makes
   explicit.
3. New reference pages: "Reading results" (annotated output per selector), a runtime/scaling
   table **measured by a committed script with provenance** (the review-time observation that
   `select_fdr` is the O(p²)-painful entry point — ~80 s at n=2000, p=2000 on the review machine —
   is scratch evidence until this script exists), a data-type support matrix, and a glossary.

**G-packaging (S/M).** `sift/py.typed` shipped via `[tool.setuptools.package-data]` +
`Typing :: Typed`; SPDX `license = "MIT"` + `license-files` with the build floor raised to
`setuptools>=77` (PEP 639 support) in `build-system.requires`; drop `setup.py` and the `wheel`
build-requires; fuller classifiers. A CI step builds the wheel, installs it into a clean venv,
and smoke-tests it: `import sift` works, `py.typed` is present in the installed package, license
metadata is present, and no `benchmarks`/`tests` top-level packages leak in. The
import-name question (`import sift` collides with Sift Science's SDK inside one environment) is
**downgraded to a documented caveat** unless PyPI publication is actually planned or a real
internal installation conflict is reproduced — per the owner's current stance of not publishing;
revisit only if that changes (§3.5).

**Implementation status.** G-packaging is complete in the working 0.9 batch: PEP 639 license
metadata, `py.typed`, package-data declaration, the `setuptools>=77` build floor, removal of the
legacy `setup.py`/`wheel` build requirement, and clean-wheel verification are implemented. Release
automation clean-installs the exact built wheel and attaches the checked source and wheel
distributions to the GitHub Release; it contains no PyPI publication job.

**G-ci (M).** `[tool.pytest.ini_options]` with markers (`slow`, `catboost`, `categorical`) and
`filterwarnings = ["error", ...allowlist]`; coverage floor; a separate min-pins job actually
exercising `numpy==1.24 / numba==0.59 / scikit-learn==1.3 / pandas==2.0` (the floors are advertised
but never executed — and D1's version gates get exercised for free). A separate Python 3.13
support job runs on the supported dependency set; it is not described as a Python-3.13/min-pins
combination unless that exact matrix cell is added. Both jobs use pip caching, `timeout-minutes`,
and `concurrency`; benchmark runs compare against **stored baselines** with host/BLAS/version
provenance instead of same-run A/B; the B0 gate summarizer is wired into the scheduled benchmark
job.

---

## 3. Open decisions (owner)

Review recommendations adopted as the working defaults; each stays overridable.

| # | decision | working default |
| --- | --- | --- |
| 1 | `select_fdr` statistic | keep `relevance`; F8c bakeoff decides any 1.0 flip |
| 2 | `select_cefsplus(k=75)` | keep through 0.9; 1.0 behavior decided after 0.9 telemetry (warning on implicit default is a candidate ledger entry, not committed) |
| 3 | blocks vs columns for `k` (F3) | `k` counts blocks |
| 4 | multiclass helpers | defer; document the support matrix instead |
| 5 | import/package name | **settled**: owner confirmed no PyPI publication; no rename, documented caveat only |
| 6 | `transform` output order | preserve each selector's legacy order through 0.9: filter path/selection order, Boruta original order, and Stability descending selection frequency with stable original-index ties; `output_order="original"` is opt-in now and the default flips in 1.0 |
| 7 | sklearn floor | stay 1.3 in 0.9; raise to 1.4 in 1.0 (enables un-gating routing tests) |

## 4. Deprecation ledger (everything that flips in 1.0)

| item | 0.9 state | 1.0 state |
| --- | --- | --- |
| `random_state` defaults (`None`/`42` sites) | `None` sites warn on default use; `42` sites remain literal `42` with no sentinel or omission warning | `0` everywhere |
| `verbose` default | `True`, logging-backed; logging formatting/routing may differ, but selection/returns/default progress behavior does not | `False` |
| `n_jobs=-1` defaults (stability, permutation, CatBoost) | unchanged, documented | `1` |
| `transform` output order | `"legacy"` default: filter path/selection order, Boruta original order, Stability descending selection frequency with stable original-index ties; `"original"` opt-in | `"original"` default |
| `sift.__all__` | 58 names in 0.9.0 (55 existing + 3 additions); later 0.9.x additions are explicitly counted or module-scoped; experimental names warn | §2.C allowlist (42 + explicitly counted landed F additions) |
| `select_cached` tuple returns | unchanged + `return_result=True` added | tuples deprecated |
| CatBoost `group_col`/`sample_weight_col` | aliases beside arrays | removed |
| stability `alpha` | joined by `penalty=` alias | `alpha` removed |
| overlapping `AutoKConfig` fields | audited + documented in 0.9; no deprecation | consolidated only where the audit proves equivalence |
| `AutoKConfig` option groups | flat dataclass fields are canonical; group objects are builders/read-only views and are not simultaneous constructor fields | same storage contract unless a separately versioned API is approved |
| `StabilitySelector.selected_features_` index/name asymmetry | unchanged, documented; `view.features` is the unified accessor | unchanged unless separately decided — **not** auto-flipped |
| sklearn floor | 1.3 | 1.4 |

## 5. Sequencing

Adopts the review's staging; each milestone is shippable and keeps the contract matrix green.

| milestone | contents | size |
| --- | --- | --- |
| **Stage 0** | the seven baseline defects (§2.0), including the smart-sampler `feature_names` subset contract — **fixed and tested with this rev** | S |
| **0.9.0a1 — complete** | B0 contract matrix + deprecation infra + gate summarizer → B1 logging/warning/callback cleanup → G-packaging — *zero default-path algorithmic selection, return-type, or default changes*. Code and dual-environment focused acceptance are complete. The dated clean-commit D9 timing/provenance and mean-oracle gate artifacts are recorded; the unrecoverable mixed-convention legacy artifact stays frozen. | ~1.5 weeks |
| **0.9.0b1** | A additive `SelectionView` + adapters → B2 convention additions → C presets/option groups/`sift.experimental` (surface unchanged) | ~2 weeks |
| **0.9.0** | D sklearn integration (version-gated) → E `target_cv` (reusing sklearn `TargetEncoder`) → G-docs consolidation → G-ci | ~3 weeks |
| **0.9.x** | F1, F2, F7, F8a → F8b/F8c (validated aggregation + bakeoff) → F3 blocks → E4 one-hot → F9 compare (leakage-safe) → manifests → F4 `Stabilized` → F5 multi-target → F6 `ModelSelector` | incremental |

Ordering constraints from review, encoded above: blocks before one-hot; leakage-safe comparison
before manifests; validated knockoff aggregation before generic stabilization.

## 6. Definition of done for 0.9

1. Contract matrix green: every 0.8-style call has identical selections, return types, and output
   ordering; legacy warning kinds/counts are unchanged except for the explicitly enumerated new
   compatibility/deprecation warnings, each represented in the matrix.
2. `sift.as_result(...)` yields the same five working accessor lines on filter, knockoff, Boruta,
   CatBoost, stability, path-evaluation, and permutation-importance outputs; legacy result types
   untouched.
3. `make_pipeline(MRMRSelector(k=20, task="regression", verbose=False), Ridge())` fits,
   transforms with feature names, and honors `set_output(transform="pandas")`. On sklearn ≥1.4
   with routing enabled, the acceptance test is:

   ```python
   selector = CEFSPlusSelector(
       k="auto", auto_k_config=AutoKConfig.predictive(strategy="group_cv")
   ).set_fit_request(groups=True)          # routing requests are explicit, not implied
   cross_validate(make_pipeline(selector, Ridge()), X, y, params={"groups": g})
   ```

   Requests must be set explicitly (or via documented class-level defaults) — routing does not
   forward `groups` on its own — and fixed-`k` selectors reject `groups` by design since 0.8, so
   the test must use a group-aware auto-k configuration.
4. `grep -rc "print(" sift/` → 0; the deprecation ledger (§4) is reproduced verbatim in the
   release notes; every alias has a warn-and-forward test.
5. A DataFrame with string columns has a one-argument, leakage-safe path through every filter
   selector with no optional dependencies. Where the calling context accepts `groups`/`time`
   (auto-k, selector classes, `Stabilized`, `sift.compare`), the encoder's folds respect them;
   fixed-`k` function calls accept neither (by design, since 0.8), so the encoder uses plain
   K-fold there.
6. Every public export has a numpydoc docstring and a generated reference page; every manual code
   block executes in CI; the runtime table and Auto-K gate table are regenerated by committed,
   tested scripts.
