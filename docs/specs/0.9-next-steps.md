# SIFT 0.9 — consolidated next steps

Status: active execution plan. **Stages 1 and 2 are complete**; Stage 3 (documentation and release
readiness) is the open work. Verified against `stage1/0.9-corrections` at `6017df6` (PR #72), the
last head with a full green CI run, plus the adversarial-review rounds recorded at the end of this
document.

This document reconciles the Fable review, the Grok review, and independent local
reproduction. Current code and reproducible behavior win wherever the reviews disagree. It is an
execution companion to [the product-layer campaign](0.9-product-layer.md), not a replacement for
that specification.

## Current state

- The 0.9 stack now runs on GitHub/Linux CI: it is on `stage1/0.9-corrections` (PR #72), whose
  required jobs are green on `6017df6`. The bullet below records the pre-CI state it replaced.
- ~~Local `main` is 16 commits ahead of `origin/main` (`be99df0`). None of the 0.9 commits has run
  on GitHub/Linux CI.~~ Superseded by the PR #72 run.
- The package version is `0.9.0a1`; the release notes currently lead with an unreleased
  `0.9.0` section.
- Local evidence is strong but not release evidence: the base suite passed 1,573 tests with 23
  skipped, and the Python 3.11 / NumPy 2.2 / sklearn 1.7 suite passed 1,666 with 16 skipped.
- The repository is not published to PyPI. The asset-only GitHub-release workflow that replaces
  `python-publish.yml` is **on the default branch** since PR #71 (`d10afef`), so the protection is
  live: a release event builds and attaches artifacts and never publishes to PyPI. Publication to
  PyPI remains out of scope for 0.9.0, and creating a `v0.9.0` tag/GitHub Release stays an
  explicit owner action rather than part of routine implementation.
- Stage 0, B0-B2, SelectionView, Auto-K ergonomics, sklearn integration, and target encoding
  E1/E2 are implemented. The target encoder's unique-category behavior invalidated the claim that
  E was complete; Stage 1 §1.1/§1.2 closed it (see those sections for evidence), so E is now
  complete. Correctness repair still precedes all new feature work.

## Decisions from the review

| ID | Finding | Decision |
| --- | --- | --- |
| C1 | Unique `target_cv` levels become fold markers and can outrank noise | **Fixed (§1.1).** Independently reproduced: `id` entered mRMR's top three in 7/8 seeds (8/8 on the pinned fixture). Centered emissions bring it to 0/8. |
| C2 | Binary encoding metadata is recomputed and can report inactive folds; missing requested categorical columns can raise a rich-result `KeyError` | **Fixed (§1.2).** Actual encoder metadata now flows through `BinaryPathRun` and the classic/Gaussian paths. |
| C3 | `allow_full_data_target_encoding=True` is silently ignored with `target_cv` | **Fixed (§1.2).** The contradictory combination raises at every entry point. |
| C4 | `KnockoffSelector` accepts `target_cv`, while `select_fdr` does not | **Fixed (§1.2).** `target_cv` is rejected in knockoffs; legacy supervised encodings warn and report `fdr_control="none"`. Function parity was not used as the fix. |
| C5 | Duck-typed sklearn inputs crash in `np.iscomplexobj`, and DataFrame feature names are stored as a list | **Fix before push.** The duck-array crash reproduces under both NumPy 1.26/sklearn 1.5 and NumPy 2.2/sklearn 1.7. Fixed in §1.3. |
| C6 | `CEFSPlusBinarySelector` lacks its legacy binary-only tag | **Fix before push.** Do not fabricate an unsupported new-style transformer tag. Fixed in §1.3. |
| R1 | Stability result views ignore `output_order` | **Fix before push.** View names, indices, and transform must match the fitted selector. |
| R2 | Auto-K filter producers discard rankings/curves they already compute | **Fix before push.** Persist producer-side data and expose the standard curve schema. |
| R3 | Knockoff results cannot establish raw input width after constant-column filtering | **Fix before push.** Add distinct raw-width provenance; do not reinterpret the existing valid-feature count. |
| R4 | JSON conversion stringifies mapping keys and falls back to arbitrary `repr()` | **Fix before push.** Use collision-free typed mapping serialization and reject unsupported arbitrary objects. |
| D1 | `input_kind="unknown"` on legacy result-only views | **No change.** This is the documented honest provenance state. |
| D2 | `warmup_policy="exclude"` and `"zero_weight"` behave identically | **No numerical change.** They are documented row-preserving aliases; physical row deletion is incompatible with transformer shape. |
| D3 | `view.py` and Auto-K modules are very large | **Defer the split until after the correction/CI release gate.** This is maintenance debt, not a correctness blocker. |
| D4 | G-CI has not started | **Rejected as stated.** Wheel/release smoke and the gate summarizer exist; the remaining CI work is listed below. |
| D5 | Percentage-complete estimate for the 0.9 campaign | **Do not use one.** Track the explicit stage gates below; a single percentage obscures the difference between the core 0.9.0 release and the later 0.9.x capability program. |

## Stage 1 — correctness and contract repair — **complete**

All four items (§1.1 centering, §1.2 routing/metadata, §1.3 sklearn contracts, §1.4 result views)
are done, and the completion gate below is met. No new capability work started before it closed.

### 1.1 Center target encodings on fold-local priors — **done**

Emit centered category effects rather than raw fold target means:

- OOF training: `encoded_effect = fold_encoding - fold_training_prior`.
- Inference: `encoded_effect = full_fit_encoding - full_training_prior`.
- An unseen or unique category therefore emits zero, the centered equivalent of the raw global
  prior, rather than a value identifying its fold.
- Apply the rule to fixed-fold encoding and SIFT's weighted, group, and time encoders.
- Keep smoothing, group exclusion, strict-history time folds, tied timestamps, effective weights,
  and the one-raw-column/one-encoded-column contract unchanged.
- Earliest temporal rows with an explicit target-independent prior emit a centered neutral effect;
  without one they retain zero effective selection weight.
- Update user-facing wording from “unknown maps to the global mean” to “unknown maps to zero
  centered effect (the raw global-mean estimate before centering).”

**Design decision taken during implementation:** all `target_cv` routing moves to SIFT's own
engine. sklearn's `TargetEncoder` never exposes the per-fold training priors centering must
subtract, so a two-engine design could not carry one guarantee. Fixed-k split construction
(`KFold`/`StratifiedKFold(n_splits, shuffle=True, random_state=...)`), the effective split count,
the seed, and `smooth="auto"` empirical-Bayes shrinkage are all preserved — the reimplemented
`"auto"` matches sklearn to 1.8e-15 on unweighted data and generalizes to weighted rows by
replacing integer counts with weighted row mass. Recorded in
[the campaign spec](0.9-product-layer.md) §E implementation status.

Acceptance (all covered by `tests/contracts/test_target_cv_encoding.py`, plus release-gate
coverage in `tests/test_release_readiness.py`):

1. A unique-ID column has one constant OOF value and zero variance.
   `test_unique_id_column_has_one_constant_oof_value_and_zero_variance`.
2. The ID has zero relevance and never outranks a nonconstant noise feature. Do not require its
   exclusion when `k` exhausts all nonconstant candidates.
   `test_unique_id_never_outranks_a_nonconstant_noise_feature_in_mrmr` (k ∈ {2, 3} × 8 seeds),
   `test_unique_id_carries_zero_relevance_in_the_reported_ranking`,
   `test_unique_id_never_outranks_noise_on_cefsplus_routes`
   (`penalized_objective`, `gaussian_cv`, and the no-config router), and the explicit carve-out
   `test_requesting_every_column_still_succeeds_without_requiring_exclusion`.
3. Grouped and temporal proxies do not gain relevance from complement-fold priors.
   `test_group_proxy_gains_no_relevance_from_complement_fold_priors` and
   `test_timestamp_proxy_gains_no_relevance_from_complement_fold_priors`.
4. Mutating held-out group/future targets leaves the corresponding centered effects unchanged.
   `test_mutating_held_out_group_targets_leaves_centered_effects_unchanged` and
   `test_mutating_future_targets_leaves_earlier_centered_effects_unchanged`.
5. Known, missing, and unseen inference categories remain deterministic after pickle round-trip.
   `test_inference_categories_are_deterministic_after_pickle_round_trip`.

**Evidence.** On the reproduced 600-row, 8-seed fixture (unique `id`, 8-level `city` effect, `x1`,
`x_noise`, K=5), `select_mrmr(k=3, cat_encoding="target_cv")` selected `id` in 8/8 seeds with
`corr(enc(id), y)` between −0.061 and −0.133 before the fix; after centering the column is a
zero-variance constant, `corr` is exactly 0, and `id` is selected in 0/8 seeds. Group proxies fell
from `|corr|` 0.383 to 0.000 and timestamp proxies from 0.966 to 0.000. Four existing tests whose
pinned values assumed raw-prior emissions were updated: the two time-fold fixtures now carry two
categories so centering leaves a real contrast (a single-category column correctly collapses to
zero, pinned separately), the unknown/missing inference test pins the zero centered effect instead
of `encoder_.target_mean_`, and the OOF-vs-inference selector test uses a low-cardinality column
because a unique-ID column is now constant and therefore not a selectable candidate.

### 1.2 Repair target-encoding routing and metadata — **done**

- Preserve only the nested metadata shape
  `encoding_cv={"kind": ..., "n_splits": ...}`; remove stray top-level `kind` and `n_splits`
  currently emitted by classic and Gaussian function results.
- Carry the encoder's actual binary metadata through `BinaryPathRun`; do not recompute split counts
  from all rows when zero-weight rows were inactive.
- Pin the reproduced binary time-fold fixture: with six timestamp blocks and two zero-weight
  blocks, both the fitted encoder and the public rich result report
  `encoding_cv={"kind": "time", "n_splits": 4}`. The current reconstructed value of five is a
  regression.
- Attach encoding metadata only when categorical encoding was actually applied. A requested but
  absent categorical column must not cause `return_result=True` to fail.
- Reject `target_cv` together with `allow_full_data_target_encoding=True` across function,
  selector, binary, and Boruta entry points.
- Reject `target_cv` in `KnockoffSelector`. Retain 0.8 supervised-encoding compatibility only with
  an explicit warning and `fdr_control="none"` metadata, because target-derived preprocessing does
  not retain the Model-X claim.
- Keep fixed-k `groups`/`time` rejection, multiclass rejection, and the default
  `cat_encoding="none"` unchanged.

**Evidence.** `_target_cv_metadata`'s request-side reconstruction is deleted; the only producer of
encoding metadata is `target_cv_metadata_from_encoder`, fed by the fitted encoder's `encoding_cv_`
through `ClassicPrepared.target_cv_metadata`, the Gaussian cache builder, and the new
`BinaryPathRun.encoding_cv` field. Covered by
`test_target_cv_metadata_emits_no_stray_top_level_fold_keys` (all four function routes),
`test_binary_time_route_reports_the_encoders_active_fold_count` (encoder and public rich result
both `{"kind": "time", "n_splits": 4}` on the six-block/two-zero-weight fixture; the reconstructed
value was five), and `test_requested_but_absent_categorical_column_does_not_break_rich_results`
(previously `KeyError: 'encoding_cv'` on the binary route). The requested-but-absent convention was
determined by reproduction: `loo`/`loo_logit` ignore an absent `cat_features` column silently on
both the binary and filter routes, so `target_cv` now does the same rather than raising. C3 is
covered by `test_function_routes_reject_target_cv_with_full_data_escape_hatch` and
`test_selector_classes_and_boruta_reject_the_full_data_escape_hatch`; C4 by
`test_knockoff_selector_rejects_target_cv`,
`test_knockoff_legacy_supervised_encoding_warns_and_drops_the_fdr_claim`, and the scoping guard
`test_knockoff_without_supervised_encoding_keeps_its_fdr_claim`.

### 1.3 Close the sklearn regressions — **done**

- Make dense validation safe for array-like/duck-typed input without invoking an unsupported
  `__array_function__`; detect complex dtype after safe materialization.
- Use sklearn-compatible wording: `Complex data not supported ...`.
- Store `feature_names_in_` as sklearn's required one-dimensional NumPy object array for both
  named and positional fits. Preserve generated `x0...` names in that public attribute for
  positional fits because this is existing 0.8 behavior; use a separate provenance marker where
  code must distinguish named from positional input. Apply the shared convention to filter
  selectors, Knockoff, Boruta, and Stability.
- Set the legacy `binary_only=True` tag for `CEFSPlusBinarySelector`. On sklearn's object-tag API,
  document and test the nearest valid representation rather than turning the transformer into a
  classifier merely to obtain a tag.
- Pin `check_sample_weights_not_an_array`, `check_transformer_data_not_an_array`,
  `check_complex_data`, and `check_dataframe_column_names_consistency` where applicable.

Evidence (NumPy 1.26 / pandas 2.2 / sklearn 1.5.1, `check_estimator(generate_only=True)`
run per class, sklearn's own `SkipTest` outcomes counted separately):

| Class | Before (pass/skip/fail) | After (pass/skip/fail) |
| --- | --- | --- |
| `MRMRSelector` | 39 / 0 / 4 | 42 / 0 / 1 |
| `JMISelector` | 39 / 0 / 4 | 42 / 0 / 1 |
| `JMIMSelector` | 39 / 0 / 4 | 42 / 0 / 1 |
| `CEFSPlusSelector` | 38 / 0 / 5 | 41 / 0 / 2 |
| `CEFSPlusBinarySelector` | 27 / 0 / 16 | 41 / 0 / 2 |
| `KnockoffSelector` | 33 / 3 / 5 | 35 / 4 / 2 |
| `BorutaSelector` | 39 / 0 / 4 | 42 / 0 / 1 |
| `StabilitySelector` | 36 / 0 / 7 | 39 / 0 / 4 |

`check_sample_weights_not_an_array`, `check_transformer_data_not_an_array`, and
`check_complex_data` now pass for every class (Knockoff's transformer-equivalence check is
skipped by sklearn itself because the estimator is tagged non-deterministic).
`check_dataframe_column_names_consistency` passes for the six order-strict transforms; it is
not applicable to `BorutaSelector` and `StabilitySelector`, whose `transform` deliberately
selects fitted columns by name, so those two pin the ndarray/object-dtype/content half of the
contract directly. Residual failures are pre-existing and outside 1.3: `check_requires_y_none`
and `check_fit2d_1sample` message wording, plus `StabilitySelector`'s
`check_sample_weights_invariance`, `check_dtype_object`, and `check_fit2d_predict1d`.

Focused suites plus the full base suite pass (1,612 passed, 25 skipped); ruff and
`git diff --check` are clean.

### 1.4 Complete the additive result-view contract — **done**

Evidence (base environment: Python 3.12, NumPy 1.26, pandas 2.2, sklearn 1.5.1):

- R1: `tests/test_stability_result_view.py::test_view_follows_the_fitted_output_order`
  and `::test_non_default_output_order_actually_reorders_the_view` pin both orders and
  prove they are not trivially identical.
- R2: `tests/test_selection_view.py::test_auto_k_filter_view_is_complete_and_carries_a_normalized_curve`,
  `::test_auto_k_routes_publish_their_criterion_curve` (ten routes),
  `::test_routes_without_a_k_curve_say_why`, `::test_auto_k_router_curve_follows_the_routed_method`,
  `::test_classic_auto_k_retains_ranking_and_curve`, and
  `::test_fixed_k_filter_view_keeps_legacy_ranking_and_no_curve`.
- R3: `tests/test_selection_view.py::test_knockoff_metadata_separates_raw_and_post_filter_width`,
  `::test_knockoff_view_builds_raw_support_without_input_features`, and
  `::test_legacy_knockoff_results_without_raw_width_stay_partial`.
- R4: `tests/test_selection_view.py::test_mixed_key_mapping_survives_a_json_round_trip`,
  `::test_ordinary_string_key_mappings_keep_their_plain_json_shape`,
  `::test_view_payload_root_and_metadata_stay_plain_json_objects`, and
  `::test_unsupported_objects_raise_instead_of_leaking_repr`.
- Full suite: 1,573 passed, 23 skipped. Ruff and `git diff --check` clean.
- D1 (`input_kind="unknown"`) and D3 (no `view.py` split) are unchanged.

- Stability views must apply the selector's `output_order` consistently to `features`, `indices`,
  `path_rank`, and frozen `transform` behavior.
- Auto-K producers must retain a complete feature ranking and a normalized curve with exactly
  `k`, `criterion`, `criterion_se`, and `selected`. Adapters consume the normalized payload rather
  than guessing method-specific diagnostics.
- Knockoff metadata gains `n_features_input` distinct from the existing post-filter
  `n_features`. Use it to create the raw support width; tables remain explicitly partial for
  dropped columns unless raw rows are available.
- JSON schema version 1 must not silently merge keys such as `1` and `"1"` or serialize memory
  addresses. Keep ordinary string-key mappings, including the payload root and normal metadata,
  as JSON objects. Only a mapping containing non-string keys uses a tagged, ordered key/value
  envelope with typed keys. Raise a clear `TypeError` for unsupported arbitrary objects. Tests
  must preserve both mixed-key entries through JSON dump/load while leaving the ordinary payload
  shape unchanged.
- Keep honest `input_kind="unknown"` behavior. Do not split `view.py` in this correction stage.

### Stage 1 completion gate — **met**

- Focused leakage, metadata, sklearn-check, and result-view suites pass in the base and sklearn
  1.7 environments. **Met**, and the whole declared band was subsequently exercised (see Stage 2's
  latest-dependency row and `docs/development.md`).
- The two existing full suites pass with no unexplained selection regressions. **Met.**
- Ruff, `compileall`, documentation smoke tests, and `git diff --check` are clean. **Met.**
- The campaign spec and release notes no longer claim completion for behavior the tests disprove.
  **Met**, with one correction from adversarial-review round 1: the §1.1 write-up over-claimed
  the scope of centering. Centering neutralizes only unseen-in-fold emissions, and the docs now
  say so explicitly (see the review rounds below, round 1 finding 2).

## Stage 2 — first CI-backed integration — **complete**

1. **Done.** Create a topic branch from the verified local stack and open a PR; do not push local
   `main` directly. The stack is on `stage1/0.9-corrections` as PR #72.
2. **Done.** The no-PyPI workflow replacement was split out and merged first, as PR #71
   (`d10afef`), so the asset-only release workflow is already on the default branch and no release
   event can publish to PyPI.
3. **Done, except the deferred 3.13 job.** Complete the minimum CI surface:
   - Python 3.10, 3.11, and 3.12 on Linux — kept;
   - latest supported NumPy 2 / sklearn 1.7 coverage — the scheduled
     `test-latest-deps` canary is kept;
   - an actual minimum-pins job for every direct runtime floor: NumPy 1.24, pandas 2.0,
     sklearn 1.3, Numba 0.59, SciPy 1.10, joblib 1.3, and threadpoolctl 3.1 — added as
     `min-pins` (Python 3.10 in CI) and **pre-validated locally on Python 3.11: 1,566
     passed, 30 skipped**. The floors co-install and are mutually consistent (numba 0.59
     pins numpy `<1.27`), so no floor in `pyproject.toml` needs raising;
   - CatBoost and optional categorical dependency jobs — kept, and `test-catboost` now
     also asserts that the `catboost`/`categorical` markers actually select tests. The
     adversarial-review round 1 tightened that step: it counts each marker separately and
     fails on a zero count, instead of a single `--collect-only -m "catboost or
     categorical"` that passed while either marker alone still matched something;
   - wheel build and clean-install smoke — kept;
   - Python 3.13 on a supported dependency set — **deferred with evidence, and the
     evidence has since changed.** numba ships cp313 wheels from 0.61.0, and a local
     3.13.15 run with numba 0.67 reached 1,565 passed / 3 failed. The blockers were
     sklearn 1.9 (10 failures) and numpy 2.5 (3 failures), which broke the existing
     3.11/3.12 matrix identically because the declared ranges resolve to them. All 13
     are now closed (Stage 2 — latest-dependency compatibility): the newest resolution
     `pyproject.toml` allows — numpy 2.5.2 / pandas 3.0.5 / sklearn 1.9.0 / scipy 1.18.1
     / numba 0.67.0 on Python 3.12 — is green at 1,680 passed / 30 skipped, so the whole
     declared band is supported and no ceiling was added. `docs/development.md` records
     the verified band and the ready-to-enable job definition; the 3.13 job itself is
     still unenabled and its first run should be read rather than assumed. It is now
     deferred **only** for want of a CI run, and it needs one 3.13-specific pin,
     `numba>=0.61` (below that numba ships no cp313 wheel). The workflow comment stating
     the old blockers was stale and was rewritten in adversarial-review round 1;
   - pip caching, job timeouts, and workflow concurrency — added to every job, with
     `cancel-in-progress` scoped to pull-request runs;
   - the deterministic Auto-K gate summarizer in the scheduled benchmark job — wired in
     after `run_benchmarks.py`, regenerating the gate table from the committed raw CSVs,
     checking it with `summarize_auto_k_gates.py --verify-against`, and uploading it as
     `sift-auto-k-gate-table`. Verified locally and in CI. The check is **numeric, not a
     byte-for-byte `cmp`**: floats are rendered with 12 significant digits and compared at
     `rtol=1e-9`, every other cell exactly, because a raw `repr` differs in the 17th digit
     between macOS/arm64 and Linux CI. The job checks out with `fetch-depth: 0` because
     the summarizer verifies its provenance sidecar against the commit that sidecar names.
4. **Done.** Add pytest markers and an audited warning-error policy. Do not blindly enable
   `filterwarnings=error`: classify current warnings and keep only narrow, justified allowlist
   entries.
   - `testpaths = ["tests"]` plus registered `slow`, `catboost`, and `categorical` markers.
   - All 53 baseline warnings were classified. The allowlist holds exactly one entry — the
     loky `fork()` `DeprecationWarning` — and no category is blanket-ignored. Everything
     else was fixed at the site: `pytest.warns` assertions, local
     `@pytest.mark.filterwarnings`, explicit seeds where a `FutureWarning` asked for one,
     and removal of `AutoKConfig` fields the chosen `k_method` provably does not consume.
   - The suite is green under the final policy in three environments: the base env
     (1,573 passed / 23 skipped), the min-pins floor (1,566 / 30), and
     numpy 2.4.6 / pandas 2.3.3 / sklearn 1.7.2 with pytest 9.1.1 (1,568 / 28).
   - The audit surfaced two latent CI failures that pytest 7.4.4 had been hiding: three
     `pytest.warns` assertions in `tests/test_knockoff_filter.py` that pass only because
     pytest 7 discarded a second legitimate advisory, and a hard `import matplotlib` in
     `tests/test_stability_selection.py` for a dependency no extra declares.
5. When platform-sensitive golden tests fail, replace exact values with invariants only after the
   CI evidence shows a legitimate numerical/platform difference. Do not loosen failures
   pre-emptively.

Stage 2 is complete only when every required job is green on the exact PR head and the workflow
artifact/wheel provenance points to that commit.

### Stage 2 completion evidence — **met**

PR #72, green head `6017df6`. Every required job green: `test (3.10)`, `test (3.11)`,
`test (3.12)`, `test-catboost`, `min-pins`, and `wheel-smoke`. `test-latest-deps` and
`benchmark-smoke` are schedule/dispatch-only and are not required for the PR gate. The
`test (3.12)` job installs the newest resolution `pyproject.toml` allows, so the top of the
declared dependency band is exercised on every run rather than only in the scheduled canary.

The gate first went green at `4de1957`; the two adversarial-review commits that followed
(`020e082`, `6017df6`) each re-ran it green, so `6017df6` is the head the evidence above refers to.
Heads pushed after `6017df6` — including the offset-target numerics fix in round 3 below — need
their own run before Stage 2's "green on the exact PR head" condition holds again.

Four CI-only fixes were needed to reach the first green — none of them changed selection behavior:

1. **Full-history checkout.** `tests/test_auto_k_gate_summary.py` verifies the committed gate
   table's provenance sidecar by hashing recorded sources *at the commit the sidecar names*, which
   a depth-1 clone cannot resolve. Every job that runs the suite now checks out with
   `fetch-depth: 0`; the test skips with an explicit message on a shallow clone rather than
   failing.
2. **The nested auto-k stub keyword.** A test double did not accept the keyword the nested auto-k
   route passes, which reproduced only under the CI dependency resolution.
3. **CatBoost seed warnings.** Under the warnings-as-errors policy, CatBoost paths emitted
   seed-related advisories that the local base environment did not surface; they are handled at
   the site, per the audited policy, not blanket-ignored.
4. **Platform-stable gate rendering.** The gate table's float cells were rendered with a raw
   `repr`, which differs in the 17th digit between macOS/arm64 and Linux CI. They are now rendered
   with 12 significant digits and verified numerically at `rtol=1e-9` (`4de1957`).

## Adversarial review rounds (post-`4de1957`)

### Round 1 — six findings (fixed in `020e082`)

An independent adversarial review (Grok) of PR #72 raised six findings, all reproduced locally
against `4de1957` before being fixed. None changed selection numerics.

| # | Severity | Finding | Resolution |
| --- | --- | --- | --- |
| 1 | medium | Weighted `target_cv_smoothing="auto"` was advertised but unreachable: `_validate_smoothing` rejected `"auto"` for every weighted, grouped, or time-aware call, so `select_mrmr(..., cat_encoding="target_cv", sample_weight=...)` and `select_cefsplus_binary(..., class_weight="balanced")` failed on the default smoothing. | **Fixed.** `"auto"` is accepted on every fold kind; the empirical-Bayes prior is defined by weighted row mass and documented in `TargetCVEncoder`. Integer-weight replication is exact (0.0 difference) and the full-fit map still matches sklearn to 2.8e-17. No contextual case is undefined for `"auto"` that is defined for an explicit float; the one genuinely undefined case, a fitting slice with no positive weight mass, still raises for both and is pinned. |
| 2 | medium | Centering was over-claimed. Strictly unique IDs and 1:1 group/time proxies are exactly zero, but a level with 2-3 training rows and a shared latent target still carries sibling rows' targets: 300 IDs x 2 rows gave `corr(enc, y) ≈ 0.88` and `select_mrmr(k=2)` picked `id`. | **Documented, numerics deliberately unchanged** — that is genuine cross-row information, not the fold marker §1.1 closed. Release notes, the `TargetCVEncoder` docstring, `DOCS.MD`, `docs/user-guide.md`, and §E now state that centering neutralizes only unseen-in-fold emissions and recommend dropping ID-like columns or passing `groups=`. Three tests pin the boundary, including that `groups=` drives the same column to exactly zero. |
| 3 | medium | The `.github/workflows/test.yml` 3.13 comment still claimed sklearn 1.9 / NumPy 2.5 break the 3.11/3.12 matrix; the band is green. The `test-catboost` marker check was also weaker than it looked. | **Fixed.** The comment now matches `docs/development.md`: 3.13 is deferred only for want of a CI run, and needs the `numba>=0.61` pin. The marker step counts each marker separately and fails on a zero count. |
| 4 | low | `TargetCVEncoder.fit()` reported `n_splits_` over all rows while `fit_transform` used active rows: 10 rows, `cv=5`, first 6 weights zero gave 5 versus 4. | **Fixed.** Both routes share `_active_split_count`; regression and binary cases are pinned. |
| 5 | low | Release notes and `docs/development.md` said the gate table is `cmp`'d byte-identical; the job runs `--verify-against` (12 significant digits, `rtol=1e-9`). | **Fixed** in the release notes, `docs/development.md`, `benchmarks/README.md`, §B0 of the campaign spec, and this document. |
| 6 | low | `feature_names_in_` is now an object ndarray, so `== ["a","b"]` is element-wise and asserting on it raises. | **Fixed:** migration note added to the Stage 1 sklearn-contracts release-notes subsection. |

### Round 2 — verification leftovers (fixed in `6017df6`)

Verifying round 1's fixes turned up three statements that round 1 had left behind. None changed
selection numerics.

| # | Severity | Finding | Resolution |
| --- | --- | --- | --- |
| 7 | low | `docs/API.md` still told callers that weighted, grouped, and time-aware `target_cv` needs an explicit smoothing float — exactly the claim finding 1 had removed from the code. | **Fixed.** The sentence now says `"auto"` is accepted on every fold kind. |
| 8 | low | The near-unique shared-target boundary test asserted `"id" in` a two-of-two selection, which cannot fail. | **Fixed.** It now asserts a `k=1` selection, so the documented boundary is falsifiable. |
| 9 | low | The workflow comment claimed the whole test matrix installs the NumPy 2.5.2 band; only the 3.12 job can. | **Fixed** in `.github/workflows/test.yml`. |

### Round 3 — offset-target numerics in `smooth="auto"` (this commit)

One P1 numerical defect, found by re-deriving the empirical-Bayes shrinkage rather than by review
of the prose. Unlike rounds 1 and 2, **this one does change numerics** — on offset targets, which
is the point.

| # | Severity | Finding | Resolution |
| --- | --- | --- | --- |
| 10 | **high** | `target_cv_smoothing="auto"` accumulated its per-category and global target moments from raw weighted sums of `y` and `y²`, so `ssd_i = Σw·y² − w_i·mean_i²` suffered catastrophic cancellation on an offset target. Measured on a 300-row / 6-level regression fixture, `fit_transform(X, y)` versus `fit_transform(X, y + 1e8)`: 0.185 max difference in the centered OOF encoding on the plain path, 0.076 weighted (integer), 0.112 weighted (float), 0.170 grouped, 0.306 time-aware, and 0.051 in the target-blind `transform`. On two near-tie designs the error decided the selection: `select_mrmr(k=1, cat_encoding="target_cv")` flipped `["cat"] → ["numeric"]` (encoding difference 0.157) and `select_cefsplus(k=1)` flipped `["numeric"] → ["cat"]` (0.230). An explicit `smooth=5.0` differed by only 4.4e-8 at the same offset, which localized the defect to the empirical-Bayes path. | **Fixed.** Every moment is now accumulated on `y - prior`, the within-category sums of squared deviations use a two-pass weighted formula (`_weighted_group_ssd`) instead of `E[y²] − E[y]²`, the shrinkage is applied in centered space so the final `estimate − prior` cancellation disappears too, and `_centered_targets` refines the weighted prior once so its own rounding error does not leak into the effects. One engine, so the training-fold OOF path, the full-fit inference path, and the weighted/grouped/time-aware fold kinds all inherit the fix. Post-fix: ≤5.6e-16 against the target the shifted run actually sees, ≤1.9e-9 against raw `y` (float64 resolves only ~1.5e-8 near 1e8, so `y + 1e8` cannot carry `y` more exactly), and both selector flips are gone. sklearn `smooth="auto"` parity holds at 8.9e-16; ordinary-scale results are unchanged to the last ulp; binary `target_cv` is pinned unchanged. |

## Stage 3 — documentation and release readiness

### 3.1 Resolve the deprecation-policy contradiction

The campaign cannot promise a 1.0 warning cycle while leaving slated aliases silent. This is a
release gate, but it does not block the first CI-backed integration of the correctness repairs.

- Wire caller-facing `FutureWarning`s for removals that can be distinguished without changing
  defaults or object identity.
- If a planned removal cannot receive a real 0.9 warning path while preserving the compatibility
  matrix, defer or remove that flip from the 1.0 ledger instead of claiming it is deprecated.
- Add production warn-and-forward tests; helper-only tests are insufficient.
- Reproduce the final, internally consistent ledger verbatim in the release notes.

### 3.2 Complete documentation and release preparation

- Refresh README with `SelectionView`/`as_result`, `target_cv`, `output_order`, metadata routing,
  Auto-K presets/option groups, and `sift.experimental`.
- Replace stale `TODO.MD` version, test-count, target-encoding, and release text.
- Consolidate the release notes into one truthful `0.9.0` section, reproduce the deprecation
  ledger, and state that 0.9.0 supersedes the never-released 0.8.0 work.
- Complete the documented G-docs gate: substantive numpydoc docstrings for every public export, a
  generated reference, and execution of every manual code block in CI. If any item is deliberately
  moved to 0.9.1, amend the campaign Definition of Done before calling 0.9.0 ready.
- Keep `__version__="0.9.0a1"` through integration. Change it to `0.9.0` only in a small release
  PR after Stage 2 is green.
- No PyPI publication. The asset-only workflow is already on the default branch (PR #71,
  `d10afef`), so a release event can only build and attach artifacts; a `v0.9.0` tag/GitHub
  Release remains an explicit owner action after the release PR merges, not part of routine
  implementation.
- After any requested release, bump the development version to `0.9.1.dev0` in a separate commit.

## Stage 4 — resume the 0.9.x capability campaign

After the release gate, reduce collision risk before adding more large features:

1. Split `selection/view.py`, `selection/filter_auto_k.py`, and `selection/auto_k.py` along their
   existing adapter/router boundaries without changing behavior.
2. Add leakage-free unsupervised `ordinal` and `frequency` categorical fallbacks.
3. Continue the accepted capability order:
   - F1 conditioning sets;
   - F2 proxy/cluster reports;
   - F7 panel within/between transforms;
   - F8a knockoff feasibility/UX;
   - F8b/F8c validated aggregation and statistic bakeoff;
   - F3 block-aware selection;
   - E4 one-hot encoding, only after F3 makes a block the selection unit;
   - F9 leakage-safe `sift.compare`, with selector fitting inside every fold;
   - reproducibility manifests;
   - F4 generic `Stabilized`;
   - F5 multi-target selection;
   - F6 generic `ModelSelector`.

Each capability remains independently gated by its API contract, quality evidence, documentation,
and focused tests. A clean release gate does not authorize changing the documented `select_fdr`
statistic or `k=75` defaults; those remain evidence-backed owner decisions.

## Immediate next action

Stages 1 and 2 are closed, so the next action is Stage 3: resolve the deprecation ledger (§3.1)
before any release-ready decision, then complete documentation and release preparation (§3.2).
Keep `__version__="0.9.0a1"` until Stage 3 closes.

~~Implement Stage 1 in three reviewable commits: target encoding, sklearn contracts, and result
views. Run the focused suites between commits, then run both full environments once at the Stage
1 boundary. Only then begin the CI/PR stage; resolve the deprecation ledger in Stage 3 before the
release-ready decision.~~ Done; retained for the record.
